---
phase: 06-contact-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/contact/ContactSection.tsx
  - components/contact/MapEmbed.tsx
  - app/(main)/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor sees Google Maps showing store at 5301 Farmville Rd on the homepage"
    - "Visitor can click Get Directions and Google Maps opens with driving directions"
    - "Clicking See In Store on any product scrolls to the contact section"
    - "Contact section shows store address, hours, and phone number"
  artifacts:
    - path: "components/contact/ContactSection.tsx"
      provides: "Full contact section with id=contact, map, store info, directions"
      contains: "id=\"contact\""
    - path: "components/contact/MapEmbed.tsx"
      provides: "Google Maps iframe embed"
      contains: "google.com/maps/embed"
    - path: "app/(main)/page.tsx"
      provides: "Homepage with ContactSection integrated"
      contains: "ContactSection"
  key_links:
    - from: "components/products/ProductCard.tsx"
      to: "components/contact/ContactSection.tsx"
      via: "anchor link href=#contact to id=contact"
      pattern: "id=\"contact\""
    - from: "components/contact/MapEmbed.tsx"
      to: "Google Maps Embed API"
      via: "iframe src with API key"
      pattern: "google\\.com/maps/embed"
---

<objective>
Build the contact section with Google Maps embed, store info, directions button, and wire it into the homepage. This resolves the #contact anchor target that Phase 4 product CTAs already reference. The form column will show a "call us" fallback that Plan 02 replaces with the real contact form.

Purpose: Gives visitors a map, directions, and store details -- the #1 conversion path for a local furniture store.
Output: ContactSection and MapEmbed components, homepage updated with contact section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-contact-conversion/06-RESEARCH.md

@app/(main)/page.tsx
@components/hero/HeroSection.tsx
@components/Footer.tsx
@components/products/ProductCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MapEmbed and ContactSection components</name>
  <files>components/contact/MapEmbed.tsx, components/contact/ContactSection.tsx</files>
  <action>
Create two Server Components in `components/contact/`:

**MapEmbed.tsx** (Server Component):
- Google Maps iframe embed using Maps Embed API place mode
- src: `https://www.google.com/maps/embed/v1/place?key=${process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY}&q=5301+Farmville+Rd,Farmville,VA+23901`
- Container: `aspect-[16/9] w-full overflow-hidden rounded-lg`
- iframe attributes: `width="100%" height="100%" style={{ border: 0 }} allowFullScreen loading="lazy" referrerPolicy="no-referrer-when-downgrade"`
- title: "Town & Country Furniture location at 5301 Farmville Rd, Farmville, VA"
- If API key env var is missing, render a fallback div with the address text and a link to Google Maps (graceful degradation for dev/missing key)

**ContactSection.tsx** (Server Component):
- Section element with `id="contact"` (this is the anchor target for ProductCard "See In Store" CTAs)
- aria-label: "Contact and location"
- Background: `bg-surface` (matches alternating section pattern from Phase 4)
- Padding: `py-16`
- Section header: "Visit Our Showroom" heading with `fontFamily: 'var(--font-display)'`, subtitle about La-Z-Boy and Ashley collections
- Two-column grid layout: `grid grid-cols-1 lg:grid-cols-2 gap-12`
- **Left column** (map and info):
  - MapEmbed component
  - Store address as `<address>` element (semantic HTML)
  - Store hours list (Mon-Fri 9am-5:30pm, Sat 9am-5pm, Closed Sunday)
  - Phone number as click-to-call link: `<a href="tel:+14342238163">`
  - "Get Directions" button: `<a href="https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent('5301 Farmville Rd, Farmville, VA 23901')}" target="_blank" rel="noopener noreferrer">` with inline SVG map icon (same pattern as HeroSection)
  - Style the directions button: `inline-flex items-center justify-center px-6 py-3 bg-accent text-white font-medium rounded-lg hover:bg-accent-hover transition-colors`
- **Right column** (form placeholder for now):
  - "Send Us a Message" heading with display font
  - Paragraph: "Have a question about our furniture? Fill out the form below and we'll get back to you within 24 hours."
  - Temporary fallback: "Or call us directly at" with phone link. This will be replaced by ContactForm in Plan 02.
  - Wrap the placeholder in a div with a comment: `{/* Plan 02 replaces this with ContactForm */}`

Follow existing component patterns:
- Use `var(--font-display)` inline style for headings (same as Footer, HeroSection, ProductCard)
- Use project oklch color tokens: text-foreground, text-muted, bg-accent, etc.
- Use Tailwind spacing consistent with other sections (max-w-7xl mx-auto px-6)
  </action>
  <verify>
Run `npx next build` (or `npm run build`) -- no TypeScript or build errors. Visually confirm ContactSection.tsx has `id="contact"` and MapEmbed.tsx has the Google Maps iframe src.
  </verify>
  <done>ContactSection and MapEmbed components exist with correct structure, anchor ID, and styling consistent with existing components.</done>
</task>

<task type="auto">
  <name>Task 2: Add ContactSection to homepage</name>
  <files>app/(main)/page.tsx</files>
  <action>
Import and add ContactSection to the homepage:

1. Add import: `import { ContactSection } from "@/components/contact/ContactSection";`
2. Add `<ContactSection />` after `<VideoShowcase />` and before the closing `</main>` tag
3. The homepage component order should be: HeroSection, TrustStrip, CategoryGrid, LaZBoySection, AshleySection, VideoShowcase, ContactSection

This is the only file change. The ContactSection's `id="contact"` now resolves the anchor target that ProductCard's `href="#contact"` links have been pointing to since Phase 4. The existing `scroll-padding-top: 5rem` in globals.css handles the sticky nav offset automatically.

Do NOT modify any other components. The existing "See In Store" CTAs in ProductCard.tsx, the "Get Directions" link in HeroSection.tsx, and the "Call Us" button all already work -- they just needed this anchor target to exist.
  </action>
  <verify>
Run `npm run build` -- no errors. Then `npm run dev` and verify:
1. Homepage shows contact section at the bottom (before footer)
2. Scrolling down reveals map embed, store info, and directions button
3. Click any "See In Store" button on a product card -- page scrolls to contact section with heading visible (not hidden under nav)
  </verify>
  <done>ContactSection renders on homepage. ProductCard "See In Store" anchors scroll to the contact section. Get Directions button opens Google Maps.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- Homepage renders ContactSection after VideoShowcase
- `id="contact"` exists in the DOM
- Clicking `#contact` anchor links scrolls to the section with proper offset
- Google Maps iframe loads (requires NEXT_PUBLIC_GOOGLE_MAPS_API_KEY env var; fallback shows if missing)
- "Get Directions" link opens Google Maps with correct destination
- Phone number is clickable
- Store hours display correctly
</verification>

<success_criteria>
1. A Google Maps embed shows the store location at 5301 Farmville Rd on the homepage
2. A "Get Directions" button opens Google Maps driving directions to the store
3. Store address, hours, and phone number are visible in the contact section
4. "See In Store" CTAs from product cards scroll to the contact section
</success_criteria>

<output>
After completion, create `.planning/phases/06-contact-conversion/06-01-SUMMARY.md`
</output>
