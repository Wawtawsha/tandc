---
phase: 03-homepage-hero-trust-strip
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/categories/CategoryGrid.tsx
  - app/(main)/page.tsx
autonomous: true

must_haves:
  truths:
    - "Product category grid displays six visual cards (Recliners, Sofas, Sectionals, Lift Chairs, Living Room, Bedroom) with links to showcase sections"
    - "Homepage composes all three sections in order: HeroSection, TrustStrip, CategoryGrid"
    - "Homepage builds and renders without errors at localhost"
    - "Category cards have hover effects and are visually distinct even without product images"
  artifacts:
    - path: "components/categories/CategoryGrid.tsx"
      provides: "Server Component with six category cards in responsive grid"
      exports: ["CategoryGrid"]
    - path: "app/(main)/page.tsx"
      provides: "Homepage composing HeroSection + TrustStrip + CategoryGrid"
      contains: "HeroSection"
  key_links:
    - from: "app/(main)/page.tsx"
      to: "components/hero/HeroSection.tsx"
      via: "import and render"
      pattern: "import.*HeroSection"
    - from: "app/(main)/page.tsx"
      to: "components/trust/TrustStrip.tsx"
      via: "import and render"
      pattern: "import.*TrustStrip"
    - from: "app/(main)/page.tsx"
      to: "components/categories/CategoryGrid.tsx"
      via: "import and render"
      pattern: "import.*CategoryGrid"
    - from: "components/categories/CategoryGrid.tsx"
      to: "showcase sections"
      via: "href links on category cards"
      pattern: "href=.*#"
---

<objective>
Build the product category grid and compose the complete homepage by assembling all three Phase 3 sections (hero, trust strip, categories) into page.tsx.

Purpose: This completes the homepage landing experience -- after the hero captures attention and the trust strip builds credibility, the category grid guides visitors to browse by furniture type. The composed page is the final deliverable for Phase 3.

Output: CategoryGrid.tsx component and a fully rewritten page.tsx that renders all three homepage sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-hero-trust-strip/03-RESEARCH.md
@.planning/phases/03-homepage-hero-trust-strip/03-01-SUMMARY.md

@app/globals.css (color tokens: accent, background, surface, foreground, muted, border)
@app/(main)/page.tsx (current placeholder homepage -- will be replaced)
@app/(main)/layout.tsx (layout shell wrapping children)
@components/hero/HeroSection.tsx (from Plan 01)
@components/trust/TrustStrip.tsx (from Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CategoryGrid component</name>
  <files>
    components/categories/CategoryGrid.tsx
  </files>
  <action>
    Create `components/categories/CategoryGrid.tsx` as a Server Component (NO 'use client'):

    1. Define a `categories` array with six items:
       ```
       { name: 'Recliners', href: '#recliners', color: 'oklch(0.55 0.12 70)' }
       { name: 'Sofas', href: '#sofas', color: 'oklch(0.50 0.10 55)' }
       { name: 'Sectionals', href: '#sectionals', color: 'oklch(0.58 0.08 80)' }
       { name: 'Lift Chairs', href: '#lift-chairs', color: 'oklch(0.52 0.14 65)' }
       { name: 'Living Room', href: '#living-room', color: 'oklch(0.48 0.10 75)' }
       { name: 'Bedroom', href: '#bedroom', color: 'oklch(0.45 0.08 60)' }
       ```
       These colors are warm-toned variations that complement the site's accent palette. The hrefs use anchor links that will resolve to product showcase sections in Phase 4. For now they are placeholder anchors.

    2. Wrap in `<section aria-labelledby="categories-heading">` with classes: `py-16 md:py-20`

    3. Inner container: `max-w-7xl mx-auto px-6`

    4. Section heading: `<h2 id="categories-heading" className="text-3xl md:text-4xl font-bold text-center mb-10 md:mb-14 text-foreground" style={{ fontFamily: 'var(--font-display)' }}>Shop by Category</h2>`

    5. Grid: `grid grid-cols-2 md:grid-cols-3 gap-4 md:gap-6`

    6. Each category card is an `<a>` element:
       - `href={cat.href}` (anchor links for now)
       - Classes: `group relative aspect-[4/3] rounded-xl overflow-hidden transition-transform duration-300 hover:scale-[1.02] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent`
       - Background: use inline `style={{ backgroundColor: cat.color }}` as placeholder until real product images are available
       - Gradient overlay: `<div className="absolute inset-0 bg-gradient-to-t from-black/60 via-black/20 to-transparent" />`
       - Category name: `<h3 className="absolute bottom-4 left-4 text-white text-lg md:text-xl font-bold drop-shadow-lg">{cat.name}</h3>`
       - Arrow indicator: a small `<span>` in the bottom-right with an arrow SVG (inline, aria-hidden) that transitions on group-hover: `absolute bottom-4 right-4 text-white/70 group-hover:text-white group-hover:translate-x-1 transition-all duration-300`

    7. Export as named export `CategoryGrid`

    NOTE: No images are used yet. The solid oklch color backgrounds with gradient overlay create visually appealing placeholder cards that won't cause layout shift when real images are added later (same aspect ratio container). When images become available, replace the `style={{ backgroundColor }}` with an `<img>` or Next.js `<Image>` using `object-cover`.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `components/categories/CategoryGrid.tsx` does NOT contain `'use client'`
    - File exports `CategoryGrid` as named export
  </verify>
  <done>
    CategoryGrid.tsx is a Server Component rendering six furniture category cards in a responsive 2x3 / 3x2 grid with placeholder color backgrounds, gradient overlays, category names, and hover effects. Cards link to anchor targets for future Phase 4 product showcases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compose homepage with all three sections</name>
  <files>
    app/(main)/page.tsx
  </files>
  <action>
    Rewrite `app/(main)/page.tsx` to compose the complete homepage:

    1. Keep the existing `Metadata` export (title: "Home", description about Town & Country)

    2. Import the three section components:
       - `import { HeroSection } from '@/components/hero/HeroSection'`
       - `import { TrustStrip } from '@/components/trust/TrustStrip'`
       - `import { CategoryGrid } from '@/components/categories/CategoryGrid'`

    3. The `Home` component renders all three in order inside `<main id="main-content">`:
       ```
       <main id="main-content">
         <HeroSection />
         <TrustStrip />
         <CategoryGrid />
       </main>
       ```

    4. This page remains a Server Component (no 'use client'). The layout.tsx already wraps children in Navigation + LenisProvider + PageTransition + Footer, so page.tsx only needs the content sections.

    5. Delete all the old placeholder content (the centered "Town & Country Furniture" text with min-h-screen section).

    IMPORTANT: page.tsx must NOT have 'use client'. It's a Server Component that composes other Server Components (HeroSection, TrustStrip, CategoryGrid). The client boundary is pushed down to HeroVideo.tsx inside HeroSection.
  </action>
  <verify>
    - `npm run build` succeeds with no errors
    - `npm run dev` serves the homepage with all three sections visible
    - `app/(main)/page.tsx` does NOT contain `'use client'`
    - `app/(main)/page.tsx` imports all three section components
    - `npx tsc --noEmit` passes
  </verify>
  <done>
    Homepage (page.tsx) composes HeroSection, TrustStrip, and CategoryGrid in order. Page is a Server Component. Build passes. Dev server renders the complete homepage with hero video, trust signals, and category grid.
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes with no errors
- `npm run dev` serves the homepage at localhost with three visible sections:
  1. Hero section with video embed placeholder, headline, and two CTA buttons
  2. Trust signal strip with four icons and labels
  3. Category grid with six colored cards
- No `'use client'` directives in page.tsx, CategoryGrid.tsx, HeroSection.tsx, or TrustStrip.tsx (only in HeroVideo.tsx)
- All anchor links in category cards are present (even if targets don't exist yet)
- Call Us button has `href="tel:+14342238163"`
- Get Directions button has correct Google Maps URL with `api=1` parameter
</verification>

<success_criteria>
- CategoryGrid server component renders six category cards (Recliners, Sofas, Sectionals, Lift Chairs, Living Room, Bedroom) with color placeholder backgrounds and hover effects
- Homepage page.tsx composes HeroSection + TrustStrip + CategoryGrid as a Server Component
- `npm run build` passes clean
- All Phase 3 success criteria from ROADMAP.md are met:
  1. Full-width hero with video, headline, Call Us and Get Directions CTAs
  2. Trust strip with Local Delivery, Family Owned, Financing Available, Authorized Dealer icons
  3. Category grid with six furniture category cards linking to showcase sections
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage-hero-trust-strip/03-02-SUMMARY.md`
</output>
