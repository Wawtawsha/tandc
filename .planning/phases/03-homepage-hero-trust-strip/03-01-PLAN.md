---
phase: 03-homepage-hero-trust-strip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components/hero/HeroVideo.tsx
  - components/hero/HeroSection.tsx
  - components/trust/TrustStrip.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage hero section displays a full-width area with embedded YouTube video (facade pattern), headline text, and two CTA buttons (Call Us, Get Directions)"
    - "Trust signal strip renders four icons with labels: Local Delivery, Family Owned, Financing Available, Authorized Dealer"
    - "HeroVideo is the only Client Component; HeroSection and TrustStrip are Server Components"
    - "Video uses react-lite-youtube-embed facade pattern (click-to-play, no autoplay, noCookie=true)"
    - "Call Us button links to tel:+14342238163, Get Directions opens Google Maps directions URL"
  artifacts:
    - path: "components/hero/HeroVideo.tsx"
      provides: "Client Component wrapping LiteYouTubeEmbed"
      contains: "'use client'"
    - path: "components/hero/HeroSection.tsx"
      provides: "Server Component with headline, CTAs, and HeroVideo child"
      exports: ["HeroSection"]
    - path: "components/trust/TrustStrip.tsx"
      provides: "Server Component with four trust signal icons from Lucide"
      exports: ["TrustStrip"]
  key_links:
    - from: "components/hero/HeroSection.tsx"
      to: "components/hero/HeroVideo.tsx"
      via: "import and render as child"
      pattern: "import.*HeroVideo"
    - from: "components/hero/HeroVideo.tsx"
      to: "react-lite-youtube-embed"
      via: "LiteYouTubeEmbed component"
      pattern: "LiteYouTubeEmbed"
    - from: "components/trust/TrustStrip.tsx"
      to: "lucide-react"
      via: "named icon imports"
      pattern: "import.*from 'lucide-react'"
---

<objective>
Install video and icon dependencies, then build the hero section (video embed + headline + CTAs) and trust signal strip as reusable components.

Purpose: These are the first two sections a visitor sees on the homepage -- the hero creates immediate visual impact with a video and clear calls to action, while the trust strip establishes credibility before they scroll further.

Output: Three new component files (HeroVideo.tsx, HeroSection.tsx, TrustStrip.tsx) ready to be composed into the homepage in Plan 02.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-hero-trust-strip/03-RESEARCH.md

@app/globals.css (color tokens: accent, background, surface, foreground, muted, border)
@hooks/useReducedMotion.ts (useSyncExternalStore pattern, React 19 compliant)
@components/Navigation.tsx (existing pattern: 'use client', motion library, useReducedMotion usage)
@app/(main)/layout.tsx (layout shell: Navigation + LenisProvider + PageTransition + Footer)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and build HeroVideo + HeroSection</name>
  <files>
    package.json
    components/hero/HeroVideo.tsx
    components/hero/HeroSection.tsx
  </files>
  <action>
    1. Run `npm install react-lite-youtube-embed lucide-react` from project root.

    2. Create `components/hero/HeroVideo.tsx` as a Client Component:
       - Add `'use client'` directive at top
       - Import `LiteYouTubeEmbed` from `react-lite-youtube-embed` and its CSS (`react-lite-youtube-embed/dist/LiteYouTubeEmbed.css`)
       - Import `useReducedMotion` from `@/hooks/useReducedMotion`
       - Accept props: `{ videoId: string; title?: string }`
       - Render `LiteYouTubeEmbed` inside a container div with `aspect-video rounded-lg overflow-hidden shadow-2xl` classes
       - Configure: `poster="maxresdefault"`, `noCookie={true}`, `params="controls=1&modestbranding=1&rel=0"`
       - If `reducedMotion` is true, still render the component (it's click-to-play anyway, no autoplay to suppress)
       - Export as named export `HeroVideo`

    3. Create `components/hero/HeroSection.tsx` as a Server Component (NO 'use client'):
       - Import `HeroVideo` from `./HeroVideo`
       - Wrap in `<section aria-labelledby="hero-heading">` with classes: `relative min-h-[60vh] md:min-h-[80vh] flex items-center py-12 md:py-20`
       - Inner container: `max-w-7xl mx-auto px-6 w-full`
       - Layout: use CSS grid `grid md:grid-cols-2 gap-8 md:gap-12 items-center`
       - Left column (text content):
         - `<h1 id="hero-heading">` with display font (`style={{ fontFamily: 'var(--font-display)' }}`), text: "Quality Furniture for Every Room" with classes `text-4xl md:text-5xl lg:text-6xl font-bold mb-4 text-foreground`
         - Subtitle `<p>` with text: "Farmville, Virginia's home for La-Z-Boy and Ashley Furniture" in `text-lg md:text-xl text-muted mb-8`
         - CTA buttons in a `flex flex-col sm:flex-row gap-4` container:
           - "Call Us" button: `<a href="tel:+14342238163">` with classes `inline-flex items-center justify-center px-6 py-3 bg-accent text-white font-semibold rounded-lg hover:bg-accent-hover transition-colors text-lg`. Include an inline SVG phone icon (5x5, stroke, aria-hidden). Add `aria-label="Call Town and Country Furniture at 4 3 4, 2 2 3, 8 1 6 3"`.
           - "Get Directions" button: `<a href="https://www.google.com/maps/dir/?api=1&destination=5301+Farmville+Rd+Farmville+VA+23901" target="_blank" rel="noopener noreferrer">` with classes `inline-flex items-center justify-center px-6 py-3 border-2 border-accent text-accent font-semibold rounded-lg hover:bg-accent hover:text-white transition-colors text-lg`. Include an inline SVG map-pin icon (5x5, stroke, aria-hidden).
       - Right column: render `<HeroVideo videoId="dQw4w9WgXcQ" title="Ashley Furniture Showroom" />` (placeholder videoId -- client will provide the real one)
       - Export as named export `HeroSection`

    IMPORTANT:
    - Do NOT add 'use client' to HeroSection.tsx. Only HeroVideo.tsx needs it.
    - Use inline SVGs for CTA icons (phone and map-pin) rather than importing from lucide-react. This keeps HeroSection as a Server Component. Lucide is reserved for TrustStrip.
    - The videoId "dQw4w9WgXcQ" is a well-known placeholder. It will be replaced with the actual Ashley marketing video ID when the client provides it.
  </action>
  <verify>
    - `npm ls react-lite-youtube-embed lucide-react` shows both installed
    - `npx tsc --noEmit` passes with no type errors
    - `components/hero/HeroVideo.tsx` contains `'use client'`
    - `components/hero/HeroSection.tsx` does NOT contain `'use client'`
    - `npm run build` succeeds (no build errors)
  </verify>
  <done>
    HeroVideo.tsx is a Client Component wrapping LiteYouTubeEmbed with click-to-play, noCookie, and maxresdefault poster. HeroSection.tsx is a Server Component rendering headline, two CTA buttons (tel: link and Google Maps directions), and the HeroVideo child. Both packages installed and types pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build TrustStrip component with Lucide icons</name>
  <files>
    components/trust/TrustStrip.tsx
  </files>
  <action>
    Create `components/trust/TrustStrip.tsx` as a Server Component (NO 'use client'):
    - Import four icons from `lucide-react`: `Truck`, `Heart`, `CreditCard`, `Award`
    - Define a `signals` array inline:
      ```
      { icon: Truck, label: 'Local Delivery', description: 'We deliver personally to your home' }
      { icon: Heart, label: 'Family Owned', description: 'Serving Farmville for generations' }
      { icon: CreditCard, label: 'Financing Available', description: 'Flexible payment options' }
      { icon: Award, label: 'Authorized Dealer', description: 'Official La-Z-Boy & Ashley partner' }
      ```
    - Wrap in `<section aria-label="Why choose Town and Country">` with classes: `bg-surface py-10 md:py-14`
    - Inner container: `max-w-7xl mx-auto px-6`
    - Grid: `grid grid-cols-2 md:grid-cols-4 gap-6 md:gap-8`
    - Each signal item:
      - Container: `flex flex-col items-center text-center gap-2`
      - Icon: render the Lucide component with `className="w-8 h-8 text-accent"` and `aria-hidden="true"` and `strokeWidth={1.5}`
      - Label: `<h3 className="text-sm md:text-base font-semibold text-foreground">{label}</h3>`
      - Description: `<p className="text-xs md:text-sm text-muted hidden md:block">{description}</p>` (descriptions hidden on mobile to keep strip compact)
    - Export as named export `TrustStrip`

    NOTE: Lucide icons work in Server Components because they are pure SVG components with no client-side state. No 'use client' needed.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `components/trust/TrustStrip.tsx` does NOT contain `'use client'`
    - `npm run build` succeeds
  </verify>
  <done>
    TrustStrip.tsx is a Server Component rendering four trust signals (Local Delivery, Family Owned, Financing Available, Authorized Dealer) with Lucide icons, labels, and descriptions in a responsive grid. Descriptions hidden on mobile for compact display.
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes with no errors
- `npx tsc --noEmit` passes with no type errors
- Three new files exist: `components/hero/HeroVideo.tsx`, `components/hero/HeroSection.tsx`, `components/trust/TrustStrip.tsx`
- Only `HeroVideo.tsx` has `'use client'` directive
- `react-lite-youtube-embed` and `lucide-react` appear in `package.json` dependencies
</verification>

<success_criteria>
- HeroVideo client component wraps LiteYouTubeEmbed with facade pattern (click-to-play, no autoplay, noCookie)
- HeroSection server component renders headline, subtitle, Call Us (tel: link) and Get Directions (Google Maps URL) CTAs, and HeroVideo child
- TrustStrip server component renders four trust signals with Lucide icons in a responsive 2x2 / 4x1 grid
- Build passes clean with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage-hero-trust-strip/03-01-SUMMARY.md`
</output>
