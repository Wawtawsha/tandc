---
phase: 05-local-identity-about
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Footer.tsx
autonomous: true

must_haves:
  truths:
    - "Authorized La-Z-Boy Dealer badge displays in the footer on every page"
    - "Ashley Furniture Partner badge displays in the footer on every page"
    - "Badges use Award icon with text label, not unofficial logo images"
  artifacts:
    - path: "components/Footer.tsx"
      provides: "Partner badges above copyright line"
      contains: "Authorized La-Z-Boy Dealer"
  key_links:
    - from: "components/Footer.tsx"
      to: "lucide-react"
      via: "Award icon import"
      pattern: "import.*Award.*from.*lucide-react"
---

<objective>
Add dual-brand partner badges (La-Z-Boy and Ashley) to the site footer.

Purpose: Partner badges establish credibility and authorized dealer status on every page. Placing them in the footer (not header) avoids adding complexity to the client-side Navigation component and follows footer best practices for partner/certification badges.

Output: Updated Footer.tsx with "Authorized La-Z-Boy Dealer" and "Ashley Furniture Partner" badges using Lucide Award icons, displayed above the copyright line on every page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-local-identity-about/05-RESEARCH.md

# Existing file to modify
@components/Footer.tsx

# Pattern reference
@components/trust/TrustStrip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add partner badges to Footer</name>
  <files>components/Footer.tsx</files>
  <action>
Modify the existing Footer.tsx to add partner badges between the 3-column grid and the copyright line.

1. Import the Award icon from lucide-react: `import { Award } from 'lucide-react';`

2. In the existing `border-t border-border-subtle mt-8 pt-6 text-center` div (currently contains only copyright), restructure to:
   - A partner badges row ABOVE the copyright paragraph
   - The existing copyright paragraph below

3. Partner badges markup:
```
<div className="flex flex-col sm:flex-row items-center justify-center gap-4 sm:gap-8 mb-4">
  <div className="flex items-center gap-2">
    <Award className="w-5 h-5 text-accent" strokeWidth={1.5} aria-hidden="true" />
    <span className="text-sm text-muted">Authorized La-Z-Boy Dealer</span>
  </div>
  <div className="flex items-center gap-2">
    <Award className="w-5 h-5 text-accent" strokeWidth={1.5} aria-hidden="true" />
    <span className="text-sm text-muted">Ashley Furniture Partner</span>
  </div>
</div>
```

4. Keep copyright paragraph exactly as-is below the badges.

Key details:
- `flex-col sm:flex-row` so badges stack on mobile, sit side-by-side on tablet+
- `gap-4 sm:gap-8` tighter gap on mobile
- `mb-4` spacing between badges and copyright
- `aria-hidden="true"` on icons (decorative, text provides meaning)
- `strokeWidth={1.5}` matches the Lucide icon convention used in TrustStrip
- `text-accent` on icons matches TrustStrip pattern
- `text-sm text-muted` on text matches footer's existing typography

Do NOT create a separate PartnerBadges component file. This is simple enough to inline -- two divs with an icon and text each. A separate component for this would be over-abstraction.

Do NOT modify the Navigation component. Badges belong in the footer.
  </action>
  <verify>
Run `npm run build` to confirm Footer compiles. Run `npm run dev` and check the footer on http://localhost:3000 -- both badge texts should appear above the copyright line. Check mobile width (< 640px) to confirm badges stack vertically. Check desktop to confirm badges are side-by-side.
  </verify>
  <done>
Footer displays "Authorized La-Z-Boy Dealer" and "Ashley Furniture Partner" badges with Award icons above the copyright line. Badges are visible on every page of the site. Responsive: stacked on mobile, inline on desktop.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Footer shows both partner badges on homepage (/)
3. Footer shows both partner badges on about page (/about)
4. Award icon renders next to each badge text
5. Badges stack on mobile (< 640px), inline on desktop
6. No new component files created (badges are inline in Footer)
7. Footer remains a Server Component (no 'use client' added)
</verification>

<success_criteria>
- TRUST-02: Both "Authorized La-Z-Boy Dealer" and "Ashley Furniture Partner" badges display in footer on every page
- Badges use text + Lucide Award icon (no unofficial logo images)
- Footer retains all existing functionality (hours, contact, links, copyright)
</success_criteria>

<output>
After completion, create `.planning/phases/05-local-identity-about/05-02-SUMMARY.md`
</output>
