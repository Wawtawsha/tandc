---
phase: 05-local-identity-about
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/about/StorySection.tsx
  - components/about/PhotoGrid.tsx
  - components/about/DeliveryPromise.tsx
  - app/(main)/about/page.tsx
autonomous: true

must_haves:
  truths:
    - "The /about page tells Town & Country's story as a family-owned Farmville business"
    - "Three photo placeholder slots display with oklch backgrounds and 'Photo Coming Soon' overlay"
    - "Local delivery messaging uses personal accountability language, not corporate shipping speak"
    - "The /about page has proper SEO metadata including title, description, and OpenGraph tags"
  artifacts:
    - path: "components/about/StorySection.tsx"
      provides: "Family history narrative with two-column layout"
      min_lines: 25
    - path: "components/about/PhotoGrid.tsx"
      provides: "3-photo placeholder grid with oklch backgrounds"
      min_lines: 25
    - path: "components/about/DeliveryPromise.tsx"
      provides: "Why Buy Local trust messaging with 3 promise cards"
      min_lines: 25
    - path: "app/(main)/about/page.tsx"
      provides: "Complete About page composing all sections"
      min_lines: 30
  key_links:
    - from: "app/(main)/about/page.tsx"
      to: "components/about/StorySection.tsx"
      via: "import and render"
      pattern: "import.*StorySection"
    - from: "app/(main)/about/page.tsx"
      to: "components/about/PhotoGrid.tsx"
      via: "import and render"
      pattern: "import.*PhotoGrid"
    - from: "app/(main)/about/page.tsx"
      to: "components/about/DeliveryPromise.tsx"
      via: "import and render"
      pattern: "import.*DeliveryPromise"
---

<objective>
Build the complete About page for Town & Country Furniture with story narrative, photo placeholders, and local delivery messaging.

Purpose: The About page is the primary trust-building asset -- it communicates that Town & Country is a real, local, family-owned business with deep community roots. This is the competitive angle that corporate furniture sites cannot replicate.

Output: A fully functional /about page with three content sections (story, photos, delivery promise), proper SEO metadata, and basic FurnitureStore JSON-LD structured data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-local-identity-about/05-RESEARCH.md

# Existing files to reference for patterns
@components/trust/TrustStrip.tsx
@components/products/ProductCard.tsx
@app/(main)/about/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StorySection and PhotoGrid components</name>
  <files>
    components/about/StorySection.tsx
    components/about/PhotoGrid.tsx
  </files>
  <action>
Create the `components/about/` directory with two Server Components (NO 'use client' directive).

**StorySection.tsx:**
- Two-column layout: left side has a large oklch placeholder image (aspect-[4/3], rounded-xl), right side has the story narrative
- Use `md:grid-cols-2 gap-8 md:gap-12 items-center` grid layout
- Heading: "Your Farmville Furniture Store" with display font (`style={{ fontFamily: 'var(--font-display)' }}`)
- Story content should be specific and local, not generic. Use realistic placeholder text that references Farmville, Prince Edward County, the family-owned nature, La-Z-Boy and Ashley partnerships. Example content structure:
  - Opening: "For over [XX] years, Town & Country Furniture has been helping Farmville families find the perfect furniture for their homes."
  - Middle: Reference being an authorized La-Z-Boy dealer and Ashley partner. Mention the personal touch -- customers know the owners by name.
  - Close: Invite to visit: "Stop by our showroom on Farmville Road and let us help you find exactly what you're looking for."
- Section wrapper: `py-16 md:py-20 bg-background` with `max-w-7xl mx-auto px-6`
- The placeholder image on the left uses `oklch(0.75 0.08 45)` background with a camera icon SVG and "Photo Coming Soon" text (same pattern as ProductCard placeholder)
- On mobile, image stacks above text (natural grid flow)

**PhotoGrid.tsx:**
- Section heading: "Visit Our Farmville Showroom" with display font
- 3-photo grid: `grid-cols-1 md:grid-cols-3 gap-6`
- Three placeholder slots with unique oklch hues:
  - "Store Exterior" -- `oklch(0.75 0.08 70)`
  - "Showroom Interior" -- `oklch(0.75 0.08 55)`
  - "Our Team" -- `oklch(0.75 0.08 80)`
- Each slot: `aspect-[4/3] rounded-xl overflow-hidden` with camera SVG icon and "Photo Coming Soon" text centered (same pattern as ProductCard lines 36-54, but use the Lucide Camera icon imported from lucide-react for consistency)
- Section wrapper: `py-16 md:py-20 bg-surface` with `max-w-7xl mx-auto px-6`
- Below the grid, add a "Get Directions to Our Store" link pointing to `https://www.google.com/maps/dir/?api=1&destination=5301+Farmville+Rd+Farmville+VA+23901` (opens in new tab with `target="_blank" rel="noopener noreferrer"`)
- Direction link styled with accent color and a right-chevron inline SVG

Both components are Server Components. No useState, no useEffect, no onClick handlers.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Verify both files exist and contain no 'use client' directive. Verify Lucide Camera import is used in PhotoGrid.
  </verify>
  <done>
StorySection renders a two-column story layout with placeholder image and local narrative text. PhotoGrid renders a 3-slot photo grid with oklch placeholders, camera icons, "Photo Coming Soon" text, and a "Get Directions" link.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DeliveryPromise component and rebuild About page</name>
  <files>
    components/about/DeliveryPromise.tsx
    app/(main)/about/page.tsx
  </files>
  <action>
**DeliveryPromise.tsx:**
Server Component (no 'use client'). "Why Buy Local?" trust messaging section with three promise cards.

- Section heading: "Why Buy Local?" with display font
- Three cards in a `grid md:grid-cols-3 gap-8` layout, each centered with icon + title + description:
  1. **Truck icon** (from lucide-react): "We Deliver Personally" / "Our team brings furniture to your Farmville home with care -- not a third-party shipping company you'll never see again."
  2. **Heart icon** (from lucide-react): "Your Neighbors, Not a Chain" / "We live in this community. When something needs attention, we're a phone call away -- not a 1-800 number."
  3. **Users icon** (from lucide-react): "Family-Owned Accountability" / "The family behind Town & Country stands behind every piece we sell. Your satisfaction is our reputation."
- Icon styling: `w-12 h-12 text-accent mx-auto mb-4` with `strokeWidth={1.5}`
- Title styling: `text-xl font-semibold mb-2`
- Description styling: `text-muted`
- Section wrapper: `py-16 md:py-20 bg-background` with `max-w-7xl mx-auto px-6`

IMPORTANT: Avoid corporate e-commerce language. No "fast shipping", no "free delivery", no "orders over $X". This is personal, local, accountability-focused language.

**app/(main)/about/page.tsx:**
Completely rebuild the existing placeholder page. This is a Server Component (no 'use client').

- Export Metadata with:
  - title: "About Us | Family Owned Furniture Store in Farmville, VA"
  - description: "Town & Country Furniture has been serving Farmville, Virginia families with quality La-Z-Boy and Ashley Furniture. Locally owned, personally delivered."
  - openGraph with title, description, url ("https://townandcountryfurniture.com/about"), type "website"

- Add basic FurnitureStore JSON-LD structured data via `<script type="application/ld+json">` with `dangerouslySetInnerHTML`:
  - @type: "FurnitureStore"
  - name, description, address (5301 Farmville Rd, Farmville, VA 23901)
  - telephone: "+14342238163"
  - openingHoursSpecification for Mon-Fri 9-5:30, Sat 9-5
  - priceRange: "$$"
  NOTE: This is basic schema. Phase 7 will expand it with geo, image, url, and more.

- Page structure (composing sections):
  1. Hero intro section (`bg-surface`): centered h1 "About Town & Country Furniture" with display font, subtitle "Your local, family-owned furniture store in Farmville, Virginia"
  2. `<StorySection />` (bg-background)
  3. `<PhotoGrid />` (bg-surface)
  4. `<DeliveryPromise />` (bg-background)

- Wrap everything in `<main id="main-content">` (matches existing pattern)
- Section backgrounds alternate: surface, background, surface, background
  </action>
  <verify>
Run `npm run build` to confirm the About page builds without errors. Then run `npm run dev` and verify http://localhost:3000/about renders all four sections (hero intro, story, photos, delivery promise). Confirm no 'use client' in any of the new files.
  </verify>
  <done>
The /about page renders four distinct sections: hero intro, family story with placeholder image, 3-photo placeholder grid with directions link, and "Why Buy Local?" delivery promise section. Page has proper SEO metadata and basic FurnitureStore JSON-LD. All components are Server Components.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `/about` route renders all four sections with proper styling
3. View page source shows JSON-LD structured data with FurnitureStore type
4. No 'use client' directive in any new component file
5. Photo placeholders use oklch backgrounds with camera icons
6. Delivery messaging uses personal/local language (no corporate-speak)
7. All headings use display font via inline style
8. Section spacing follows established pattern (py-16 md:py-20)
</verification>

<success_criteria>
- TRUST-01: About page tells Town & Country's story with Farmville-specific narrative
- TRUST-03: "We Deliver Personally" and two other local trust messages display prominently
- TRUST-04: Three photo placeholder slots with oklch backgrounds ready for client imagery
- SEO: Page metadata and basic FurnitureStore JSON-LD present
</success_criteria>

<output>
After completion, create `.planning/phases/05-local-identity-about/05-01-SUMMARY.md`
</output>
