---
phase: 04-product-showcase
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - app/(main)/page.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage displays La-Z-Boy best sellers section below the category grid"
    - "Homepage displays Ashley collections section below the La-Z-Boy section"
    - "Homepage displays marketing video showcase"
    - "CategoryGrid anchor links (#recliners, #sofas, #sectionals) scroll to correct sections"
    - "All product images use Next.js Image optimization (or placeholder fallback)"
    - "Page builds without errors and LCP target is achievable"
  artifacts:
    - path: "app/(main)/page.tsx"
      provides: "Complete homepage composing hero, trust, categories, and all product showcase sections"
      contains: "LaZBoySection"
  key_links:
    - from: "app/(main)/page.tsx"
      to: "components/products/LaZBoySection.tsx"
      via: "import and render in JSX"
      pattern: "import.*LaZBoySection"
    - from: "app/(main)/page.tsx"
      to: "components/products/AshleySection.tsx"
      via: "import and render in JSX"
      pattern: "import.*AshleySection"
    - from: "app/(main)/page.tsx"
      to: "components/products/VideoShowcase.tsx"
      via: "import and render in JSX"
      pattern: "import.*VideoShowcase"
    - from: "components/categories/CategoryGrid.tsx (#recliners)"
      to: "components/products/LaZBoySection.tsx (id=recliners)"
      via: "anchor href matches section id"
      pattern: "id=\"recliners\""
    - from: "components/categories/CategoryGrid.tsx (#sofas)"
      to: "components/products/AshleySection.tsx (id=sofas)"
      via: "anchor href matches section id"
      pattern: "id=\"sofas\""
---

<objective>
Compose all product showcase sections into the homepage and verify the complete Phase 4 integration.

Purpose: This is the wiring plan. LaZBoySection, AshleySection, and VideoShowcase exist as standalone components (Plan 02). This plan adds them to the homepage in the correct order, ensures CategoryGrid anchor links resolve to the right sections, and verifies the full page builds and renders correctly.

Output: Updated `app/(main)/page.tsx` with all product showcase sections. Working anchor navigation from category cards to product sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-product-showcase/04-RESEARCH.md
@.planning/phases/04-product-showcase/04-02-SUMMARY.md

# The file we're modifying
@app/(main)/page.tsx

# Components we're importing
@components/products/LaZBoySection.tsx
@components/products/AshleySection.tsx
@components/products/VideoShowcase.tsx

# Anchor link source
@components/categories/CategoryGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose product sections into homepage</name>
  <files>app/(main)/page.tsx</files>
  <action>
Update `app/(main)/page.tsx` to import and render the three product showcase sections.

Current page.tsx structure:
```tsx
<main id="main-content">
  <HeroSection />
  <TrustStrip />
  <CategoryGrid />
</main>
```

Updated structure (add product sections AFTER CategoryGrid):
```tsx
<main id="main-content">
  <HeroSection />
  <TrustStrip />
  <CategoryGrid />
  <LaZBoySection />
  <AshleySection />
  <VideoShowcase />
</main>
```

Add these imports at the top:
```typescript
import { LaZBoySection } from "@/components/products/LaZBoySection";
import { AshleySection } from "@/components/products/AshleySection";
import { VideoShowcase } from "@/components/products/VideoShowcase";
```

The page.tsx file MUST remain a Server Component (no 'use client'). This works because:
- LaZBoySection and AshleySection are Server Components
- VideoShowcase is a Client Component, but Server Components can render Client Components as children -- the client boundary is handled by the 'use client' directive in VideoShowcase.tsx itself

Do NOT change the existing metadata export. Do NOT modify HeroSection, TrustStrip, or CategoryGrid imports.

Section order rationale:
1. HeroSection -- video hero, primary CTAs (existing)
2. TrustStrip -- credibility signals (existing)
3. CategoryGrid -- visual navigation to sections below (existing)
4. LaZBoySection (id="recliners") -- first product section, its first image is LCP candidate below the fold
5. AshleySection (id="sofas") -- second product section
6. VideoShowcase (id="sectionals") -- marketing videos at the bottom of product content

The CategoryGrid anchor links resolve as follows:
- #recliners -> LaZBoySection (id="recliners")
- #sofas -> AshleySection (id="sofas")
- #sectionals -> VideoShowcase (id="sectionals")
- #lift-chairs, #living-room, #bedroom -> these do NOT have matching sections yet. They will remain unresolved anchors (clicking them scrolls nowhere). This is acceptable for v1 -- they will be addressed when more product content is added or in future phases.
  </action>
  <verify>
Run `npx tsc --noEmit` -- must pass with zero errors.
Run `npm run build` -- must complete successfully with no build errors.
Verify the page.tsx file has no 'use client' directive.
Verify all 6 components are imported and rendered in the correct order.
  </verify>
  <done>
Homepage renders HeroSection, TrustStrip, CategoryGrid, LaZBoySection, AshleySection, and VideoShowcase in sequence. CategoryGrid anchor links #recliners, #sofas, and #sectionals scroll to the correct product sections. Page builds successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full build and anchor link resolution</name>
  <files>app/(main)/page.tsx</files>
  <action>
Run the full verification suite for Phase 4:

1. **Build verification:** Run `npm run build` and confirm it completes with zero errors.

2. **TypeScript check:** Run `npx tsc --noEmit` and confirm zero errors.

3. **Import chain verification:** Trace the full import chain to confirm no circular dependencies:
   - page.tsx -> LaZBoySection -> ProductCard -> Product type (from data/products.ts)
   - page.tsx -> AshleySection -> ProductCard -> Product type (from data/products.ts)
   - page.tsx -> VideoShowcase -> HeroVideo

4. **Anchor ID audit:** Confirm these section IDs exist in the rendered page:
   - `id="recliners"` in LaZBoySection.tsx
   - `id="sofas"` in AshleySection.tsx
   - `id="sectionals"` in VideoShowcase.tsx

5. **Server/Client boundary check:** Confirm the component tree is correct:
   - page.tsx: Server Component (no 'use client')
   - LaZBoySection: Server Component (no 'use client')
   - AshleySection: Server Component (no 'use client')
   - ProductCard: Server Component (no 'use client')
   - VideoShowcase: Client Component ('use client' present)
   - HeroVideo: Client Component ('use client' present)

If ANY verification fails, fix the issue before marking complete. Common failure modes:
- Missing 'use client' on VideoShowcase causing "Cannot use hooks in Server Component" error
- Typo in import path (e.g., `@/components/product/` instead of `@/components/products/`)
- Missing export from a component file
  </action>
  <verify>
`npm run build` exits with code 0. `npx tsc --noEmit` exits with code 0. All three anchor IDs (#recliners, #sofas, #sectionals) are present in their respective component files.
  </verify>
  <done>
Full Phase 4 builds successfully. TypeScript compiles. All import chains resolve. Anchor IDs match CategoryGrid hrefs. Server/Client component boundaries are correct.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `npx tsc --noEmit` passes with zero errors
- Homepage renders 6 sections in order: Hero, Trust, Categories, La-Z-Boy, Ashley, Videos
- Clicking #recliners in CategoryGrid would scroll to La-Z-Boy section
- Clicking #sofas in CategoryGrid would scroll to Ashley section
- Clicking #sectionals in CategoryGrid would scroll to Video section
- No new npm packages installed
- page.tsx remains a Server Component
</verification>

<success_criteria>
1. Homepage displays La-Z-Boy best sellers section with product cards and "See In Store" CTAs
2. Homepage displays Ashley collections section with product cards and "See In Store" CTAs
3. Homepage displays marketing video showcase with 3 embedded videos
4. CategoryGrid anchor links (#recliners, #sofas, #sectionals) target the correct sections
5. Featured products are configurable by editing `data/products.ts` without code changes
6. All product images would use Next.js Image optimization when real images are added (placeholder fallback works now)
7. Full production build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-showcase/04-03-SUMMARY.md`
</output>
