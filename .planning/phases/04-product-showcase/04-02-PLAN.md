---
phase: 04-product-showcase
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - components/products/LaZBoySection.tsx
  - components/products/AshleySection.tsx
  - components/products/VideoShowcase.tsx
autonomous: true

must_haves:
  truths:
    - "La-Z-Boy best sellers section displays featured products with names and See In Store CTAs"
    - "Ashley collections section displays featured products (Next-Gen, Nuvella) with names and See In Store CTAs"
    - "Marketing videos are embedded and playable inline"
  artifacts:
    - path: "components/products/LaZBoySection.tsx"
      provides: "La-Z-Boy best sellers showcase with product grid"
      exports: ["LaZBoySection"]
    - path: "components/products/AshleySection.tsx"
      provides: "Ashley collections showcase with product grid"
      exports: ["AshleySection"]
    - path: "components/products/VideoShowcase.tsx"
      provides: "Marketing video grid reusing HeroVideo"
      exports: ["VideoShowcase"]
  key_links:
    - from: "components/products/LaZBoySection.tsx"
      to: "data/products.ts"
      via: "getLaZBoyBestSellers() filter"
      pattern: "getLaZBoyBestSellers"
    - from: "components/products/AshleySection.tsx"
      to: "data/products.ts"
      via: "getAshleyFeatured() filter"
      pattern: "getAshleyFeatured"
    - from: "components/products/LaZBoySection.tsx"
      to: "components/products/ProductCard.tsx"
      via: "renders ProductCard for each product"
      pattern: "ProductCard"
    - from: "components/products/AshleySection.tsx"
      to: "components/products/ProductCard.tsx"
      via: "renders ProductCard for each product"
      pattern: "ProductCard"
    - from: "components/products/VideoShowcase.tsx"
      to: "components/hero/HeroVideo.tsx"
      via: "reuses HeroVideo for each marketing video"
      pattern: "HeroVideo"
---

<objective>
Build the three product showcase sections: La-Z-Boy best sellers, Ashley collections, and marketing video showcase.

Purpose: These are the core content sections that visitors browse. La-Z-Boy and Ashley each get their own branded section with product grids. The video showcase embeds marketing demos (self-gliding chair, sofa comparison, outdoor furniture) using the existing HeroVideo component from Phase 3.

Output: Three Server Components ready to be composed into the homepage in Plan 03.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-product-showcase/04-RESEARCH.md
@.planning/phases/04-product-showcase/04-01-SUMMARY.md

# Existing components this plan depends on
@components/products/ProductCard.tsx
@data/products.ts
@components/hero/HeroVideo.tsx
@components/categories/CategoryGrid.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build La-Z-Boy and Ashley brand sections</name>
  <files>
    components/products/LaZBoySection.tsx
    components/products/AshleySection.tsx
  </files>
  <action>
Create two Server Components (no 'use client') for the brand showcase sections. They follow nearly identical structure but with brand-specific content.

**LaZBoySection.tsx:**

Import `ProductCard` from `@/components/products/ProductCard` and `getLaZBoyBestSellers` from `@/data/products`.

Structure:
- `<section>` with `id="recliners"` (this is the anchor target from CategoryGrid's #recliners link). Use `aria-labelledby="lazboy-heading"`.
- Padding: `py-16 md:py-20`, background: `bg-background`
- Container: `max-w-7xl mx-auto px-6`
- Section header: `<h2 id="lazboy-heading">` with text "La-Z-Boy Best Sellers", styled with `font-display` via inline style (`style={{ fontFamily: 'var(--font-display)' }}`), centered, text-3xl md:text-4xl font-bold. Subtitle paragraph below: "America's favorite recliners and furniture, hand-selected for quality and comfort. See them in our Farmville showroom."
- Product grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8`
- Map over `getLaZBoyBestSellers()`, render `<ProductCard>` for each. Pass `priority={index === 0}` to the first card only.
- Below the grid: a "Visit Us to See the Full La-Z-Boy Collection" text link styled in accent color, centered, with right-arrow SVG icon. Link to `#contact`.

IMPORTANT: The `id="recliners"` on the section tag is what makes the CategoryGrid anchor link `#recliners` work. This is a critical wiring point.

**AshleySection.tsx:**

Import `ProductCard` from `@/components/products/ProductCard` and `getAshleyFeatured` from `@/data/products`.

Structure:
- `<section>` with `id="sofas"` (anchor target from CategoryGrid's #sofas link). Use `aria-labelledby="ashley-heading"`.
- Padding: `py-16 md:py-20`, background: `bg-surface` (alternating with LaZBoySection's bg-background for visual rhythm)
- Container: `max-w-7xl mx-auto px-6`
- Section header: `<h2 id="ashley-heading">` with text "Ashley Collections", same styling as LaZBoySection. Subtitle: "From the performance fabrics of Nuvella to the innovative Next-Gen collection. Visit our store to experience Ashley quality firsthand."
- Product grid: same responsive grid as LaZBoySection
- Map over `getAshleyFeatured()`, render `<ProductCard>` for each. Pass `priority={false}` to all (La-Z-Boy section is above, so its first image is the LCP candidate).
- Below the grid: "Explore Ashley Collections In Store" text link to `#contact`.

Both sections use the SAME ProductCard component. The brand badge inside ProductCard handles visual brand identification. Do NOT introduce brand-specific colors or styling beyond what ProductCard already provides.

Note on section IDs: The CategoryGrid has 6 anchors (#recliners, #sofas, #sectionals, #lift-chairs, #living-room, #bedroom). LaZBoySection gets `id="recliners"` (their primary category) and AshleySection gets `id="sofas"` (their primary category). The remaining anchors (#sectionals, #lift-chairs, #living-room, #bedroom) will be handled when the homepage is composed in Plan 03 -- but for now, these two sections claim the two most prominent anchors.
  </action>
  <verify>
Run `npx tsc --noEmit` -- should pass. Verify both files exist in `components/products/`. Verify LaZBoySection has `id="recliners"` and AshleySection has `id="sofas"`. Verify neither file has `'use client'`.
  </verify>
  <done>
LaZBoySection renders a product grid of La-Z-Boy best sellers with ProductCards and "See In Store" CTAs. AshleySection renders Ashley featured products with ProductCards. Both are Server Components with correct anchor IDs for CategoryGrid navigation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build marketing video showcase section</name>
  <files>components/products/VideoShowcase.tsx</files>
  <action>
Create `components/products/VideoShowcase.tsx`. This component MUST have `'use client'` at the top because it renders `HeroVideo` which is a Client Component. A Server Component cannot render a Client Component as a direct child in a map without being a Client Component itself (or using a wrapper pattern). Since VideoShowcase's only purpose is to render HeroVideo instances, making it a Client Component is the simplest approach.

Define a local `VideoItem` interface and a `videos` array at module level:

```typescript
interface VideoItem {
  id: string;
  title: string;
  description: string;
  videoId: string; // YouTube video ID
}

const videos: VideoItem[] = [
  {
    id: 'self-gliding-chair',
    title: 'Self-Gliding Recliner Technology',
    description: 'See how La-Z-Boy\'s self-gliding mechanism works -- smooth, effortless motion.',
    videoId: 'dQw4w9WgXcQ', // Placeholder -- replace with actual YouTube ID
  },
  {
    id: 'sofa-comparison',
    title: 'Sofa Fabric Comparison',
    description: 'Nuvella performance fabric vs traditional upholstery -- which is right for your family?',
    videoId: 'dQw4w9WgXcQ', // Placeholder -- replace with actual YouTube ID
  },
  {
    id: 'outdoor-furniture',
    title: 'Outdoor Furniture Collection',
    description: 'Ashley outdoor pieces built to last through Virginia seasons.',
    videoId: 'dQw4w9WgXcQ', // Placeholder -- replace with actual YouTube ID
  },
];
```

Structure:
- `<section>` with `id="sectionals"` (claiming the #sectionals anchor from CategoryGrid -- video showcase is placed between brand sections and contact). Use `aria-labelledby="videos-heading"`.
- Padding: `py-16 md:py-20`, background: `bg-background`
- Container: `max-w-7xl mx-auto px-6`
- Section header: `<h2 id="videos-heading">` with text "See Our Furniture In Action", same font-display styling. Subtitle: "Watch product demonstrations and learn about the features that make our furniture exceptional."
- Video grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8`
- Map over `videos`, for each render a `<div>` containing:
  - `<h3>` with `video.title`, text-lg font-semibold
  - `<p>` with `video.description`, text-sm text-muted mb-3
  - `<HeroVideo videoId={video.videoId} title={video.title} />`

Import HeroVideo from `@/components/hero/HeroVideo`. The HeroVideo component already handles the LiteYouTubeEmbed facade pattern (2KB until clicked).

The video data is hardcoded in this file (not in products.ts) because videos are marketing material, not products. They don't need the Product type system.
  </action>
  <verify>
Run `npx tsc --noEmit` -- should pass. Verify the file has `'use client'` directive. Verify it imports `HeroVideo` from `@/components/hero/HeroVideo`. Verify `id="sectionals"` is on the section element.
  </verify>
  <done>
VideoShowcase renders 3 marketing videos in a responsive grid, reusing HeroVideo's YouTube facade pattern. Each video has a title, description, and playable embed. Component is a Client Component (required because HeroVideo is a Client Component).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- LaZBoySection imports and uses `getLaZBoyBestSellers` from `@/data/products`
- AshleySection imports and uses `getAshleyFeatured` from `@/data/products`
- Both brand sections render ProductCard components
- VideoShowcase reuses HeroVideo from Phase 3
- Anchor IDs present: `id="recliners"` (LaZBoy), `id="sofas"` (Ashley), `id="sectionals"` (Videos)
- No new npm packages installed
</verification>

<success_criteria>
1. La-Z-Boy section displays best seller products with ProductCards in a responsive grid
2. Ashley section displays featured products with ProductCards in a responsive grid
3. Video showcase embeds 3 marketing videos using HeroVideo facade
4. All sections have correct anchor IDs matching CategoryGrid hrefs
5. Brand sections use "See In Store" CTAs via ProductCard (not "Buy Now")
6. Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-showcase/04-02-SUMMARY.md`
</output>
