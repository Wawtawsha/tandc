---
phase: 01-foundation-brand-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/(main)/layout.tsx
  - app/(main)/main.css
  - app/(main)/page.tsx
  - app/(main)/about/page.tsx
  - lib/fonts.ts
  - lib/metadata.ts
autonomous: true

must_haves:
  truths:
    - "The site displays warm ivory/taupe/gold colors -- NOT Shrike's dark cinematic palette"
    - "Headings render in Libre Baskerville serif font"
    - "Body text renders in DM Sans sans-serif font"
    - "The site has no dark theme class -- background is warm ivory, text is dark brown"
    - "Both / and /about routes exist and display content"
  artifacts:
    - path: "app/globals.css"
      provides: "Town & Country oklch color palette"
      contains: "oklch(0.97 0.008 80)"
    - path: "lib/fonts.ts"
      provides: "Libre Baskerville + DM Sans font configuration"
      contains: "Libre_Baskerville"
    - path: "lib/metadata.ts"
      provides: "Town & Country site metadata"
      contains: "Town & Country"
    - path: "app/(main)/about/page.tsx"
      provides: "Second route for page transition testing"
      contains: "About"
  key_links:
    - from: "app/layout.tsx"
      to: "lib/fonts.ts"
      via: "font CSS variables on body"
      pattern: "libreBaskerville\\.variable.*dmSans\\.variable"
    - from: "app/globals.css"
      to: "app/layout.tsx"
      via: "@theme font-sans references var(--font-dm-sans) set by body className"
      pattern: "--font-sans.*var\\(--font-dm-sans\\)"
    - from: "app/layout.tsx"
      to: "lib/metadata.ts"
      via: "metadata export"
      pattern: "import.*metadata.*from.*lib/metadata"
---

<objective>
Replace all Shrike branding with Town & Country's visual identity: warm oklch color palette, Libre Baskerville + DM Sans typography, T&C metadata, and adapted content. Also create an /about placeholder page so there are two routes for testing page transitions in Plan 01-03.

Purpose: This transforms a generic Shrike skeleton into a Town & Country branded application. The color palette (warm ivory, sandstone, taupe, muted gold) and typography (Libre Baskerville serif + DM Sans sans-serif) establish the store's own visual identity distinct from both La-Z-Boy and Ashley brand colors.
Output: A visually branded Next.js application with T&C colors, fonts, and metadata across all files, plus two navigable routes (/ and /about).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-brand-system/01-RESEARCH.md
@.planning/phases/01-foundation-brand-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply oklch color palette and typography system</name>
  <files>
    app/globals.css
    lib/fonts.ts
    app/layout.tsx
  </files>
  <action>
    **app/globals.css** -- Replace the ENTIRE file with Town & Country's warm oklch palette. Remove the dark mode variant. The complete file should be:

    ```css
    @import "tailwindcss";

    @theme {
      /* Town & Country warm neutral palette */
      /* Backgrounds */
      --color-background: oklch(0.97 0.008 80);
      --color-surface: oklch(0.94 0.012 75);
      --color-surface-elevated: oklch(1 0 0);

      /* Text */
      --color-foreground: oklch(0.22 0.02 55);
      --color-muted: oklch(0.48 0.02 55);
      --color-subtle: oklch(0.62 0.01 55);

      /* Accent -- muted gold */
      --color-accent: oklch(0.62 0.14 70);
      --color-accent-hover: oklch(0.56 0.16 70);

      /* Borders */
      --color-border: oklch(0.87 0.01 75);
      --color-border-subtle: oklch(0.92 0.008 75);

      /* Fonts */
      --font-sans: var(--font-dm-sans);
      --font-display: var(--font-baskerville);
    }

    html, body {
      width: 100%;
      max-width: 100vw;
    }

    .skip-to-content {
      position: absolute;
      top: -100%;
      left: 0;
      padding: 1rem 2rem;
      background: var(--color-accent);
      color: var(--color-background);
      font-weight: 600;
      z-index: 100;
      transition: top 0.2s;
    }

    .skip-to-content:focus {
      top: 0;
    }

    @media (prefers-reduced-motion: no-preference) {
      html {
        scroll-behavior: smooth;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      *,
      *::before,
      *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
        scroll-behavior: auto !important;
      }
    }
    ```

    CRITICAL CHANGES vs Shrike:
    - REMOVED: `@custom-variant dark (&:where(.dark, .dark *));` -- T&C is a light theme site, no dark mode
    - REPLACED: All oklch color values from dark cinematic to warm neutral
    - REPLACED: `--font-sans: var(--font-inter)` with `var(--font-dm-sans)`
    - REPLACED: `--font-display: var(--font-geist)` with `var(--font-baskerville)`
    - REMOVED: `--font-mono` -- not needed for a furniture site

    **lib/fonts.ts** -- Replace ALL 7 Shrike fonts with exactly 2 Town & Country fonts:

    ```typescript
    import { Libre_Baskerville, DM_Sans } from 'next/font/google';

    export const libreBaskerville = Libre_Baskerville({
      subsets: ['latin'],
      display: 'swap',
      weight: ['400', '700'],
      style: ['normal', 'italic'],
      variable: '--font-baskerville',
    });

    export const dmSans = DM_Sans({
      subsets: ['latin'],
      display: 'swap',
      weight: ['400', '500', '600', '700'],
      variable: '--font-dm-sans',
    });
    ```

    CRITICAL: Import from `next/font/google`. Libre_Baskerville requires explicit weight array (it's not a variable font). Include italic style for emphasis text. DM Sans is the body/UI font with 4 weights.

    **app/layout.tsx** -- Replace fonts and simplify metadata for Phase 1. The complete file:

    ```tsx
    import type { Metadata } from "next";
    import { libreBaskerville, dmSans } from "@/lib/fonts";
    import { siteMetadata, SITE_URL, generateOrganizationJsonLd } from "@/lib/metadata";
    import "./globals.css";

    export const metadata: Metadata = {
      metadataBase: new URL(SITE_URL),
      title: {
        default: siteMetadata.title,
        template: "%s | Town & Country Furniture",
      },
      description: siteMetadata.description,
      keywords: siteMetadata.keywords,
      authors: [{ name: siteMetadata.author }],
      robots: {
        index: true,
        follow: true,
        googleBot: {
          index: true,
          follow: true,
          "max-video-preview": -1,
          "max-image-preview": "large",
          "max-snippet": -1,
        },
      },
    };

    export default function RootLayout({
      children,
    }: Readonly<{
      children: React.ReactNode;
    }>) {
      const organizationJsonLd = generateOrganizationJsonLd();

      return (
        <html lang="en">
          <body
            className={`${libreBaskerville.variable} ${dmSans.variable} antialiased`}
          >
            <script
              type="application/ld+json"
              dangerouslySetInnerHTML={{ __html: JSON.stringify(organizationJsonLd) }}
            />
            <a href="#main-content" className="skip-to-content">
              Skip to content
            </a>
            {children}
          </body>
        </html>
      );
    }
    ```

    CRITICAL CHANGES vs Shrike:
    - REPLACED: 7 font imports with 2 (libreBaskerville, dmSans)
    - REPLACED: body className uses only 2 font variables instead of 7
    - REPLACED: title template uses "Town & Country Furniture"
    - REMOVED: OpenGraph and Twitter metadata (will be added in Phase 7 with proper og:image)
  </action>
  <verify>
    1. `npm run build` succeeds with zero errors (font imports resolve, metadata exports resolve)
    2. Grep lib/fonts.ts for "Libre_Baskerville" -- should find it
    3. Grep lib/fonts.ts for "Inter\|Geist\|Source_Code_Pro\|Syne\|Cormorant_Garamond\|Playfair_Display\|Caveat" -- should find NOTHING
    4. Grep app/globals.css for "oklch(0.97 0.008 80)" -- should find background color
    5. Grep app/globals.css for "oklch(0.1" -- should find NOTHING (Shrike dark colors gone)
    6. Grep app/globals.css for "dark" -- should find NOTHING (no dark mode variant)
  </verify>
  <done>
    globals.css contains the full T&C warm oklch palette with no dark theme remnants. fonts.ts defines exactly Libre Baskerville and DM Sans. layout.tsx applies exactly 2 font variables to body and uses T&C metadata. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adapt content files and create /about route</name>
  <files>
    lib/metadata.ts
    app/(main)/layout.tsx
    app/(main)/main.css
    app/(main)/page.tsx
    app/(main)/about/page.tsx
  </files>
  <action>
    **lib/metadata.ts** -- Replace ALL Shrike content with Town & Country:

    ```typescript
    import type { Metadata } from "next";

    export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://townandcountryfarmville.com";

    export const siteMetadata = {
      title: "Town & Country Furniture | Farmville, VA",
      description:
        "Farmville, Virginia's home for La-Z-Boy recliners, Ashley Furniture, and quality home furnishings. Family-owned, locally trusted. Visit our showroom today.",
      author: "Town & Country Furniture",
      keywords: [
        "furniture store Farmville VA",
        "La-Z-Boy dealer Farmville",
        "Ashley Furniture Farmville Virginia",
        "recliners Farmville",
        "sofas Farmville VA",
        "furniture near me Farmville",
        "Town and Country Furniture",
      ],
      og: {
        type: "website" as const,
        locale: "en_US",
        siteName: "Town & Country Furniture",
      },
      twitter: {
        card: "summary_large_image" as const,
        creator: "",
      },
    };

    export function generateOrganizationJsonLd() {
      return {
        "@context": "https://schema.org",
        "@type": "FurnitureStore",
        name: "Town & Country Furniture",
        url: SITE_URL,
        description: siteMetadata.description,
        address: {
          "@type": "PostalAddress",
          streetAddress: "5301 Farmville Rd",
          addressLocality: "Farmville",
          addressRegion: "VA",
          postalCode: "23901",
          addressCountry: "US",
        },
        telephone: "+14342238163",
        openingHoursSpecification: [
          {
            "@type": "OpeningHoursSpecification",
            dayOfWeek: ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
            opens: "09:00",
            closes: "17:30",
          },
          {
            "@type": "OpeningHoursSpecification",
            dayOfWeek: "Saturday",
            opens: "09:00",
            closes: "17:00",
          },
        ],
      };
    }
    ```

    CHANGES vs Shrike:
    - REPLACED: All Shrike branding with Town & Country
    - REPLACED: SITE_URL domain
    - REPLACED: Keywords with local furniture SEO terms
    - REPLACED: Organization JSON-LD with FurnitureStore type including address, phone, hours
    - REMOVED: generateJsonLd function (was for CreativeWork, not needed)

    **app/(main)/layout.tsx** -- Remove the `dark` class from the wrapper div:

    ```tsx
    import { Navigation } from "@/components/Navigation";
    import { Footer } from "@/components/Footer";
    import LenisProvider from "@/components/LenisProvider";
    import PageTransition from "@/components/PageTransition";
    import "./main.css";

    export default function MainLayout({
      children,
    }: Readonly<{
      children: React.ReactNode;
    }>) {
      return (
        <div className="flex flex-col min-h-screen">
          <Navigation />
          <LenisProvider>
            <PageTransition>{children}</PageTransition>
          </LenisProvider>
          <Footer />
        </div>
      );
    }
    ```

    ONLY CHANGE: Remove `dark` from className. The div now has `className="flex flex-col min-h-screen"` instead of `className="dark flex flex-col min-h-screen"`. Everything else stays identical.

    **app/(main)/main.css** -- Strip all Shrike-specific styles (Nexus, gallery button), keep body/nav/lenis styles. Update comment from "dark theme" to "warm theme". Change line-height from 1.5 to 1.6 for better body text readability:

    ```css
    /* Main site styles for Town & Country warm theme */

    body {
      background: var(--color-background);
      color: var(--color-foreground);
      font-family: var(--font-sans), system-ui, sans-serif;
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      overflow-x: hidden;
    }

    /* Button hover micro-interaction utility */
    .btn-hover {
      transition: transform 0.2s ease, opacity 0.2s ease;
    }

    .btn-hover:hover {
      transform: scale(1.03);
    }

    /* Navigation link underline animation */
    .nav-link::after {
      content: '';
      position: absolute;
      bottom: -4px;
      left: 0;
      width: 100%;
      height: 2px;
      background: var(--color-accent);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 0.2s ease;
    }

    .nav-link:hover::after {
      transform: scaleX(1);
    }

    /* Lenis smooth scroll compatibility */
    html.lenis,
    html.lenis body {
      height: auto;
    }

    .lenis.lenis-smooth {
      scroll-behavior: auto;
    }

    /* Disable hover transforms for reduced motion */
    @media (prefers-reduced-motion: reduce) {
      .btn-hover:hover {
        transform: none;
      }

      .nav-link::after {
        transition: none;
      }
    }
    ```

    REMOVED vs Shrike:
    - All `.nexus-cta` styles (nexus event gallery)
    - All `.nexus-grid` styles (nexus photo grid)
    - All `.nexus-photo` styles (nexus photo card)
    - All `.nexus-coming-soon` styles
    - All `.nav-gallery-btn` styles (gallery button in nav)
    - Reduced motion rules for nexus/gallery elements

    **app/(main)/page.tsx** -- Should already be a placeholder from Plan 01-01. If it still references Shrike components, replace with:

    ```tsx
    import type { Metadata } from "next";

    export const metadata: Metadata = {
      title: "Home",
      description: "Town & Country Furniture - Farmville, Virginia's home for La-Z-Boy and Ashley Furniture.",
    };

    export default function Home() {
      return (
        <main id="main-content">
          <section className="min-h-screen flex items-center justify-center px-6">
            <div className="max-w-2xl text-center">
              <h1 className="text-5xl font-bold mb-6" style={{ fontFamily: 'var(--font-display)' }}>
                Town &amp; Country Furniture
              </h1>
              <p className="text-xl text-muted mb-4">
                Farmville, Virginia&apos;s home for La-Z-Boy and Ashley Furniture
              </p>
              <p className="text-muted">
                Family-owned. Locally trusted. Visit our showroom today.
              </p>
            </div>
          </section>
        </main>
      );
    }
    ```

    **app/(main)/about/page.tsx** -- CREATE this file (new). This provides a second route for testing page transitions:

    ```tsx
    import type { Metadata } from "next";

    export const metadata: Metadata = {
      title: "About",
      description: "About Town & Country Furniture - a family-owned furniture store in Farmville, Virginia.",
    };

    export default function About() {
      return (
        <main id="main-content">
          <section className="min-h-screen flex items-center justify-center px-6">
            <div className="max-w-2xl text-center">
              <h1 className="text-5xl font-bold mb-6" style={{ fontFamily: 'var(--font-display)' }}>
                About Us
              </h1>
              <p className="text-xl text-muted mb-4">
                Your local furniture store in Farmville, Virginia
              </p>
              <p className="text-muted">
                Town &amp; Country has been serving the Farmville community with quality furniture from trusted brands like La-Z-Boy and Ashley Furniture. This page will be expanded in Phase 5.
              </p>
            </div>
          </section>
        </main>
      );
    }
    ```

    This is a PLACEHOLDER. Phase 5 will build the real About page. But it serves a critical purpose right now: providing a second route so page transitions can be tested by navigating between / and /about.
  </action>
  <verify>
    1. `npm run build` succeeds with zero errors
    2. Grep lib/metadata.ts for "Town & Country" -- should find it
    3. Grep lib/metadata.ts for "Shrike" -- should find NOTHING
    4. Grep lib/metadata.ts for "FurnitureStore" -- should find the JSON-LD type
    5. Grep app/(main)/layout.tsx for "dark" -- should find NOTHING
    6. Grep app/(main)/main.css for "nexus" -- should find NOTHING
    7. Verify app/(main)/about/page.tsx exists
    8. Grep app/(main)/main.css for "nav-gallery-btn" -- should find NOTHING
  </verify>
  <done>
    All content files reflect Town & Country branding. metadata.ts has FurnitureStore JSON-LD with correct address/phone/hours. Main layout has no dark class. main.css has no Nexus/gallery styles. Both / and /about routes exist with placeholder content. Build succeeds.
  </done>
</task>

</tasks>

<verification>
- `npm run build` exits with code 0
- `npm run dev` starts and serves the site at localhost
- Visiting / shows "Town & Country Furniture" heading
- Visiting /about shows "About Us" heading
- No "Shrike" text appears anywhere in the rendered site or metadata
- Browser dev tools show --font-baskerville and --font-dm-sans CSS variables on body
- Browser shows warm ivory background (#F6F3EE approximate), not dark cinematic (#1a1a2e approximate)
</verification>

<success_criteria>
1. The T&C oklch palette is fully applied: warm ivory background, dark brown text, muted gold accent
2. Libre Baskerville renders on headings (serif with visible serifs)
3. DM Sans renders on body text (clean geometric sans-serif)
4. No Shrike branding remains in any file (search for "Shrike" returns nothing except possibly comments about origin)
5. Both / and /about routes render correctly
6. `npm run build` succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-brand-system/01-02-SUMMARY.md`
</output>
