---
phase: 08-responsive-polish-deployment
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Site looks correct on mobile (375px) -- verified by human"
    - "Site looks correct on desktop (1280px) -- verified by human"
    - "Keyboard navigation works through all interactive elements -- verified by human"
    - "No visual regressions from responsive and accessibility changes"
  artifacts: []
  key_links: []
---

<objective>
Final human verification of mobile responsiveness, accessibility, and overall site quality before considering Phase 8 complete.

Purpose: The responsive fixes (08-01) and accessibility pass (08-02) were automated. This plan pauses for the user to manually verify the site looks and works correctly across devices and interaction modes.
Output: Human-verified confirmation that the site is production-ready.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Mobile-first responsive fixes across all components (48px touch targets, responsive layout audit) and WCAG AA accessibility pass (focus-visible indicators, heading hierarchy, alt text, semantic HTML).
  </what-built>
  <how-to-verify>
Run `npm run dev` and open http://localhost:3000 in Chrome.

**Mobile check (375px):**
1. Open Chrome DevTools (F12) -> Toggle Device Toolbar (Ctrl+Shift+M)
2. Set viewport to 375px width (iPhone SE)
3. Homepage: Verify hero stacks vertically (text above, video below), trust strip is 2-column, category grid is 2-column, product grids are single column, contact section stacks (map above, form below)
4. Tap all buttons/links -- they should feel comfortably large (48px minimum)
5. Navigate to About page -- verify story stacks (image above, text below), photo grid is single column, delivery promises stack vertically

**Desktop check (1280px):**
6. Set viewport to 1280px or close DevTools
7. Homepage: Verify hero is 2-column (text left, video right), trust strip is 4-column, category grid is 3-column, product grids are 3-column, contact is 2-column
8. About page: Story is 2-column, photos are 3-column, promises are 3-column

**Keyboard navigation:**
9. Close DevTools. Press Tab key repeatedly from top of homepage
10. Verify: skip-to-content link appears first, then logo, then nav links, then phone number, continuing through page content
11. Every focused element should show a visible gold outline
12. Verify no element is skipped or trapped

**Quick visual scan:**
13. Check that no text is truncated, overlapping, or unreadable
14. Check that spacing looks balanced (not too cramped or too spread out on mobile)
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks good, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Human has visually verified:
1. Mobile layout (375px) -- single column, touch targets adequate
2. Desktop layout (1280px) -- multi-column grids, full layout
3. Keyboard navigation -- focus indicators visible, no traps
4. Overall visual quality -- no regressions
</verification>

<success_criteria>
- Human confirms mobile layout looks correct at 375px
- Human confirms desktop layout looks correct at 1280px
- Human confirms keyboard navigation shows focus indicators
- No visual regressions reported
</success_criteria>

<output>
After completion, create `.planning/phases/08-responsive-polish-deployment/08-04-SUMMARY.md`
</output>
