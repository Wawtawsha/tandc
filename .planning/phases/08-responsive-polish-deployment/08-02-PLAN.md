---
phase: 08-responsive-polish-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - components/Navigation.tsx
  - components/hero/HeroSection.tsx
  - components/hero/HeroVideo.tsx
  - components/trust/TrustStrip.tsx
  - components/categories/CategoryGrid.tsx
  - components/products/LaZBoySection.tsx
  - components/products/AshleySection.tsx
  - components/products/VideoShowcase.tsx
  - components/products/ProductCard.tsx
  - components/contact/ContactSection.tsx
  - components/contact/ContactForm.tsx
  - components/contact/MapEmbed.tsx
  - components/about/StorySection.tsx
  - components/about/PhotoGrid.tsx
  - components/about/DeliveryPromise.tsx
  - components/Footer.tsx
autonomous: true

must_haves:
  truths:
    - "Keyboard Tab navigates through all interactive elements with visible focus indicators"
    - "Skip-to-content link appears on first Tab press and jumps to main content"
    - "All placeholder images have descriptive alt text"
    - "Heading hierarchy is h1 then h2 then h3 with no skipped levels on each page"
    - "Reduced-motion users see no animations or transitions"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Skip-to-content link and main landmark"
      contains: "skip-to-content"
    - path: "components/Navigation.tsx"
      provides: "Focus-visible indicators on all nav links"
      contains: "focus-visible"
  key_links:
    - from: "app/layout.tsx"
      to: "#main-content"
      via: "skip-to-content anchor"
      pattern: "href.*#main-content"
    - from: "all interactive elements"
      to: "keyboard users"
      via: "focus-visible:outline"
      pattern: "focus-visible:"
---

<objective>
Perform a WCAG AA accessibility pass across the entire site: add focus-visible indicators to all interactive elements, audit heading hierarchy, verify alt text on all images/placeholders, and ensure full keyboard navigation works.

Purpose: WCAG AA compliance is a Phase 8 requirement (DSGN-05). The site already has good foundations (skip-to-content, aria-labels, reduced motion support) but needs systematic focus indicators and minor semantic HTML fixes.
Output: All interactive elements have visible focus indicators, heading hierarchy is correct, all images have proper alt text.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-responsive-polish-deployment/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focus-visible indicators to all interactive elements</name>
  <files>
    components/Navigation.tsx
    components/hero/HeroSection.tsx
    components/products/ProductCard.tsx
    components/products/LaZBoySection.tsx
    components/products/AshleySection.tsx
    components/products/VideoShowcase.tsx
    components/contact/ContactSection.tsx
    components/contact/ContactForm.tsx
    components/contact/SubmitButton.tsx
    components/about/PhotoGrid.tsx
    components/about/DeliveryPromise.tsx
    components/Footer.tsx
  </files>
  <action>
Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent` to every interactive element that doesn't already have focus-visible styles. Use `focus-visible:` (not `focus:`) so focus rings only appear for keyboard users, not mouse clicks.

Specific elements to audit and fix:

**Navigation.tsx:**
- Logo link: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent rounded`
- Desktop nav links: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent rounded`
- Desktop phone link: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent rounded`
- Mobile menu button: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent`
- Mobile menu links: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent`
- Mobile phone link: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent`

**ProductCard.tsx:**
- "See In Store" CTA link: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent rounded`

**LaZBoySection.tsx and AshleySection.tsx:**
- "Visit Us to See..." CTA link at bottom: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent rounded`

**ContactSection.tsx:**
- "Get Directions" button: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent rounded-lg`

**ContactForm.tsx:**
- Form inputs already have `focus:ring-2 focus:ring-accent` -- these are acceptable. Leave as-is (form elements benefit from focus on mouse click too).

**SubmitButton.tsx:**
- Submit button: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent`

**Footer.tsx:**
- Quick links: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent rounded`
- PhoneLink: The analytics wrapper passes className through -- verify PhoneLink, DirectionsLink wrappers pass focus classes

**PhotoGrid.tsx:**
- "Get Directions" link: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent rounded`

**CategoryGrid.tsx:**
- Already has `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent` -- verify and leave as-is.

**MapEmbed.tsx:**
- "View on Google Maps" fallback link: Add `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent rounded`

Do NOT remove existing `focus:ring-*` styles on form inputs. Only add `focus-visible:` styles to elements that lack them.
  </action>
  <verify>
Run `npm run dev`. Open site in browser. Press Tab key repeatedly to navigate through all interactive elements on the homepage. Verify a visible gold outline (accent color) appears around each focused element. Repeat on the about page.
  </verify>
  <done>Every interactive element on every page shows a visible focus ring when navigated to via keyboard Tab. Form inputs retain their existing focus:ring styles.</done>
</task>

<task type="auto">
  <name>Task 2: Audit semantic HTML, heading hierarchy, alt text, and ARIA</name>
  <files>
    app/layout.tsx
    components/hero/HeroVideo.tsx
    components/trust/TrustStrip.tsx
    components/about/StorySection.tsx
    components/about/PhotoGrid.tsx
    components/contact/MapEmbed.tsx
  </files>
  <action>
Perform a semantic HTML and accessibility audit. Fix the following:

**Heading hierarchy audit:**
- Homepage: h1 "Quality Furniture..." (HeroSection) -> h3 in TrustStrip (PROBLEM: skips h2). Change TrustStrip h3 elements to `<p>` or `<span>` with `font-semibold` since they are labels, not headings. The section has its own `aria-label` for context.
- Homepage: h2 "Shop by Category" (CategoryGrid) -> h3 in category cards -- correct.
- Homepage: h2 "La-Z-Boy Best Sellers" -> h3 product names -- correct.
- Homepage: h2 "Ashley Collections" -> h3 product names -- correct.
- Homepage: h2 "See Our Furniture" -> h3 video titles -- correct.
- Homepage: h2 "Visit Our Showroom" -> h3 "Store Information", h3 "Send Us a Message" -- correct.
- About page: h1 "About Town & Country" -> h2 "Your Farmville Furniture Store" (StorySection) -> h2 "Visit Our Farmville Showroom" (PhotoGrid) -> h2 "Why Buy Local?" (DeliveryPromise). All correct.

**Alt text audit:**
- StorySection placeholder: The oklch placeholder div has no alt text. Wrap it in a figure with `role="img"` and `aria-label="Town & Country Furniture showroom - photo coming soon"`.
- PhotoGrid placeholders: Each placeholder div needs `role="img"` and `aria-label` describing what the photo will be (e.g., "Store exterior - photo coming soon", "Showroom interior - photo coming soon", "Our team - photo coming soon").
- ProductCard placeholder: The oklch placeholder div needs `role="img"` and `aria-label` using the product's `imageAlt` field (e.g., `aria-label={product.imageAlt + ' - photo coming soon'}`).
- HeroVideo: The LiteYouTubeEmbed already has `title` prop which generates proper alt -- leave as-is.
- MapEmbed: iframe already has `title` -- good. Fallback div needs `role="img"` and `aria-label="Map showing Town & Country Furniture location"`.

**ARIA improvements:**
- TrustStrip: Already has `aria-label` on section -- good.
- ContactForm: Already has `aria-invalid`, `aria-describedby` -- good.
- Mobile menu Escape key handler: Already closes menu and returns focus -- good.

**Root layout verification:**
- Skip-to-content link exists in `app/layout.tsx` -- verify `#main-content` target exists on each page. Homepage `page.tsx` has `<main id="main-content">`. About `page.tsx` has `<main id="main-content">`. Both correct.
- Verify `lang="en"` on html element -- already present.

Only make the specific changes listed above. Do not restructure components or add unnecessary ARIA attributes. The principle is: use native HTML semantics first, ARIA only when native semantics are insufficient.
  </action>
  <verify>
Run `npm run build` to confirm no errors. In Chrome DevTools, open the Accessibility tree (Elements panel > Accessibility tab) on the homepage. Verify: heading hierarchy shows h1 -> h2 -> h3 with no skipped levels, all images/placeholders have accessible names, and landmark regions (nav, main, footer) are present.
  </verify>
  <done>Heading hierarchy is h1 -> h2 -> h3 with no skipped levels. All placeholder images have descriptive accessible names. TrustStrip labels are semantically correct (not headings). Skip-to-content link works on all pages.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. Keyboard Tab navigates through ALL interactive elements with visible gold focus rings
3. Skip-to-content link appears on first Tab press and jumps to main content area
4. Chrome Accessibility tree shows correct heading hierarchy (no skipped levels)
5. All placeholder images have accessible names in the accessibility tree
6. Reduced motion preference disables all animations (already implemented, verify not broken)
</verification>

<success_criteria>
- Every interactive element has a focus-visible indicator
- Heading hierarchy is semantically correct on all pages
- All images and placeholder elements have descriptive alt text or aria-labels
- Skip-to-content link functions correctly
- `npm run build` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-responsive-polish-deployment/08-02-SUMMARY.md`
</output>
