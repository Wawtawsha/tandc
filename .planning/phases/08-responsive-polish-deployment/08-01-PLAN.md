---
phase: 08-responsive-polish-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Navigation.tsx
  - components/Footer.tsx
  - components/products/ProductCard.tsx
  - components/categories/CategoryGrid.tsx
  - components/about/PhotoGrid.tsx
  - components/about/DeliveryPromise.tsx
  - components/contact/ContactSection.tsx
  - components/contact/SubmitButton.tsx
  - components/products/VideoShowcase.tsx
autonomous: true

must_haves:
  truths:
    - "All buttons and links have minimum 48px touch targets on mobile"
    - "Homepage renders single-column layout at 375px with no horizontal overflow"
    - "About page renders single-column layout at 375px with no horizontal overflow"
    - "All grid sections collapse to appropriate column counts at each breakpoint"
  artifacts:
    - path: "components/Navigation.tsx"
      provides: "48px touch targets on mobile menu button and mobile menu links"
      contains: "min-h-\\[48px\\]"
    - path: "components/products/ProductCard.tsx"
      provides: "48px touch target on See In Store CTA button"
      contains: "min-h-\\[48px\\]"
    - path: "components/Footer.tsx"
      provides: "48px touch targets on footer links"
      contains: "min-h-\\[48px\\]"
  key_links:
    - from: "all components"
      to: "mobile viewport"
      via: "Tailwind mobile-first responsive classes"
      pattern: "grid-cols-1|flex-col"
---

<objective>
Audit and fix mobile responsiveness across all site components, ensuring 48px minimum touch targets on every interactive element and correct single-column layouts at 375px mobile width.

Purpose: The site has not been tested on mobile yet. This plan systematically fixes touch target sizing and responsive layout issues across all components so the site is usable on phones (60%+ expected traffic).
Output: All components pass responsive audit at 375px/768px/1280px breakpoints with 48px minimum touch targets.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-responsive-polish-deployment/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix touch targets on all interactive elements</name>
  <files>
    components/Navigation.tsx
    components/Footer.tsx
    components/products/ProductCard.tsx
    components/categories/CategoryGrid.tsx
    components/about/PhotoGrid.tsx
    components/contact/SubmitButton.tsx
  </files>
  <action>
Audit and fix every interactive element (button, link, tap target) to meet 48px minimum touch target size. Use `min-h-[48px]` and appropriate padding. Specific fixes needed:

**Navigation.tsx:**
- Mobile menu button: Change `p-2` to `p-3 min-h-[48px] min-w-[48px] flex items-center justify-center` (currently ~40px)
- Mobile menu links: Change `py-2 px-4` to `py-3 px-4 min-h-[48px] flex items-center` (ensure 48px touch height)
- Mobile phone link: Already `py-3 px-4` -- add `min-h-[48px] flex items-center` for explicit sizing
- Desktop nav links: Desktop uses mouse precision so touch targets less critical, but ensure `py-2` minimum for consistency
- Desktop phone link: Add `min-h-[48px] flex items-center` for touch-capable laptops

**Footer.tsx:**
- Footer quick links: Add `min-h-[48px] flex items-center` to the Link elements (currently just text size, no height guarantee)
- PhoneLink in footer: Add `min-h-[48px] inline-flex items-center` to ensure tap target
- Partner badge area: Not interactive, skip

**ProductCard.tsx:**
- "See In Store" CTA button: Change `px-4 py-2` to `px-4 py-3 min-h-[48px]` (currently ~40px with py-2)

**CategoryGrid.tsx:**
- Category cards are full-size aspect-ratio elements so touch target is inherently large -- no change needed

**PhotoGrid.tsx:**
- "Get Directions" link: Add `min-h-[48px] inline-flex items-center` (currently just text link)

**SubmitButton.tsx:**
- Submit button: Already `px-6 py-3` and full width, which gives ~48px. Add explicit `min-h-[48px]` for safety.

Do NOT change visual appearance. Only add minimum sizing constraints. Use `min-h-[48px]` (not `h-12`) to allow content to grow beyond 48px naturally.
  </action>
  <verify>
Run `npm run build` to confirm no type/syntax errors. Visually verify at 375px: open Chrome DevTools device mode at 375px width and check that all buttons/links are comfortably tappable (no tiny targets).
  </verify>
  <done>Every interactive element (button, link, form control) across all components has min-h-[48px] or equivalent 48px+ touch target sizing.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix responsive layouts at all breakpoints</name>
  <files>
    components/contact/ContactSection.tsx
    components/products/VideoShowcase.tsx
    components/about/DeliveryPromise.tsx
    components/about/StorySection.tsx
  </files>
  <action>
Test the site at 375px (mobile), 768px (tablet), and 1280px (desktop) in the dev server. Fix any responsive layout issues found. Known patterns to verify and fix:

**ContactSection.tsx:**
- Currently `grid-cols-1 lg:grid-cols-2` -- this is correct, but the contact form container has `p-6 lg:p-8`. Verify it doesn't cause horizontal overflow at 375px. The `gap-12` might be too large on mobile -- change to `gap-8 lg:gap-12`.

**VideoShowcase.tsx:**
- Video grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` is correct. But verify that video titles and descriptions have readable text sizing on mobile. The h3 titles have no responsive sizing -- add `text-lg md:text-xl` pattern for consistency.

**DeliveryPromise.tsx:**
- Grid is `grid md:grid-cols-3` which means mobile is single column. Verify icons and text don't look too spread out. The icon size `w-12 h-12` is fine.

**StorySection.tsx:**
- Grid is `grid md:grid-cols-2` -- on mobile this stacks with image on top, text below. Verify the story content `space-y-4` doesn't create excessive whitespace on mobile.

**General checks across all components:**
- Verify no horizontal overflow at 375px on any page (check `max-w-7xl mx-auto px-6` containers don't overflow)
- Verify `px-6` (24px each side) leaves adequate content width at 375px (375 - 48 = 327px content area)
- If any section uses `px-6` and a child has additional padding causing overflow, reduce inner padding on mobile

Run `npm run dev` and test all three pages (/, /about) at 375px, 768px, 1280px viewport widths in browser DevTools.
  </action>
  <verify>
Open dev server. In Chrome DevTools, test at 375px width: navigate through homepage and about page. Verify no horizontal scrollbar appears, all grids collapse to single column on mobile, and text is readable. Repeat at 768px and 1280px.
  </verify>
  <done>Homepage and About page render correctly at 375px (single column, no overflow), 768px (2-column where appropriate), and 1280px (full desktop layout). No horizontal scrollbar at any breakpoint.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. At 375px viewport: all interactive elements are 48px+ tap targets, layouts are single-column, no horizontal overflow
3. At 768px viewport: grids show 2-3 columns where designed, no overflow
4. At 1280px viewport: full desktop layout renders correctly
</verification>

<success_criteria>
- Every button, link, and interactive element has a minimum 48px touch target
- All pages render correctly at mobile (375px), tablet (768px), and desktop (1280px)
- No horizontal overflow or scroll at any viewport width
- `npm run build` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-responsive-polish-deployment/08-01-SUMMARY.md`
</output>
