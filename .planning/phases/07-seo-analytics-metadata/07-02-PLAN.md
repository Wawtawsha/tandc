---
phase: 07-seo-analytics-metadata
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - components/analytics/PhoneLink.tsx
  - components/analytics/DirectionsLink.tsx
  - components/analytics/CategoryLink.tsx
  - components/hero/HeroSection.tsx
  - components/contact/ContactSection.tsx
  - components/Footer.tsx
  - components/Navigation.tsx
  - components/categories/CategoryGrid.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking any phone number fires a phone_click GA4 event with phone_number and click_location parameters"
    - "Clicking any Get Directions link fires a directions_click GA4 event with click_location parameter"
    - "Clicking a category card fires a category_click GA4 event with category_name and click_location parameters"
    - "All tracked links still navigate correctly (tel: dials, directions opens Google Maps, categories scroll)"
  artifacts:
    - path: "components/analytics/PhoneLink.tsx"
      provides: "Client wrapper for tel: links with GA4 tracking"
      contains: "sendGAEvent"
    - path: "components/analytics/DirectionsLink.tsx"
      provides: "Client wrapper for directions links with GA4 tracking"
      contains: "sendGAEvent"
    - path: "components/analytics/CategoryLink.tsx"
      provides: "Client wrapper for category anchor links with GA4 tracking"
      contains: "sendGAEvent"
  key_links:
    - from: "components/analytics/PhoneLink.tsx"
      to: "@next/third-parties/google"
      via: "sendGAEvent import"
      pattern: "sendGAEvent.*phone_click"
    - from: "components/hero/HeroSection.tsx"
      to: "components/analytics/PhoneLink.tsx"
      via: "import and render"
      pattern: "PhoneLink"
    - from: "components/contact/ContactSection.tsx"
      to: "components/analytics/DirectionsLink.tsx"
      via: "import and render"
      pattern: "DirectionsLink"
    - from: "components/categories/CategoryGrid.tsx"
      to: "components/analytics/CategoryLink.tsx"
      via: "import and render"
      pattern: "CategoryLink"
---

<objective>
Create client component wrappers for tracking phone calls, directions clicks, and category clicks, then wire them into all existing Server Components that have untracked links. This covers the phone_click, directions_click, and category_click events from the GA4 tracking plan.

Purpose: Server Components (HeroSection, ContactSection, Footer, CategoryGrid) cannot have onClick handlers. Client wrappers add sendGAEvent tracking while preserving the original link behavior and styling.
Output: Three tracking wrapper components. Six existing components updated to use them.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-seo-analytics-metadata/07-RESEARCH.md
@.planning/phases/07-seo-analytics-metadata/07-01-SUMMARY.md

# Components to update
@components/hero/HeroSection.tsx
@components/contact/ContactSection.tsx
@components/Footer.tsx
@components/Navigation.tsx
@components/categories/CategoryGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PhoneLink, DirectionsLink, and CategoryLink tracking wrappers</name>
  <files>components/analytics/PhoneLink.tsx, components/analytics/DirectionsLink.tsx, components/analytics/CategoryLink.tsx</files>
  <action>
  Create three client component wrappers in components/analytics/:

  **PhoneLink.tsx:**
  ```
  'use client';
  import { sendGAEvent } from '@next/third-parties/google';

  interface PhoneLinkProps {
    phone: string;
    location: string;
    children: React.ReactNode;
    className?: string;
    ariaLabel?: string;
  }

  export function PhoneLink({ phone, location, children, className, ariaLabel }: PhoneLinkProps) {
    return (
      <a
        href={`tel:${phone}`}
        className={className}
        aria-label={ariaLabel}
        onClick={() => {
          sendGAEvent('event', 'phone_click', {
            phone_number: phone,
            click_location: location,
          });
        }}
      >
        {children}
      </a>
    );
  }
  ```

  **DirectionsLink.tsx:**
  ```
  'use client';
  import { sendGAEvent } from '@next/third-parties/google';

  interface DirectionsLinkProps {
    destination: string;
    location: string;
    children: React.ReactNode;
    className?: string;
    newTab?: boolean;
  }

  export function DirectionsLink({ destination, location, children, className, newTab = true }: DirectionsLinkProps) {
    const href = `https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(destination)}`;
    return (
      <a
        href={href}
        className={className}
        target={newTab ? '_blank' : undefined}
        rel={newTab ? 'noopener noreferrer' : undefined}
        onClick={() => {
          sendGAEvent('event', 'directions_click', {
            click_location: location,
            destination_address: destination,
          });
        }}
      >
        {children}
      </a>
    );
  }
  ```

  **CategoryLink.tsx:**
  ```
  'use client';
  import { sendGAEvent } from '@next/third-parties/google';

  interface CategoryLinkProps {
    href: string;
    categoryName: string;
    children: React.ReactNode;
    className?: string;
    style?: React.CSSProperties;
  }

  export function CategoryLink({ href, categoryName, children, className, style }: CategoryLinkProps) {
    return (
      <a
        href={href}
        className={className}
        style={style}
        onClick={() => {
          sendGAEvent('event', 'category_click', {
            category_name: categoryName,
            click_location: 'homepage_grid',
          });
        }}
      >
        {children}
      </a>
    );
  }
  ```

  Keep the wrappers minimal. They accept children for maximum flexibility -- the parent component controls the visual content, the wrapper just adds the onClick + href behavior.
  </action>
  <verify>
  Check that all three files exist in components/analytics/. Each file has 'use client' directive, imports sendGAEvent from '@next/third-parties/google', and exports a named component. Run `npx tsc --noEmit` or `npm run build` to verify no TypeScript errors.
  </verify>
  <done>
  Three tracking wrapper components created: PhoneLink (phone_click event), DirectionsLink (directions_click event), CategoryLink (category_click event). All use sendGAEvent from @next/third-parties/google.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire tracking wrappers into existing components</name>
  <files>components/hero/HeroSection.tsx, components/contact/ContactSection.tsx, components/Footer.tsx, components/Navigation.tsx, components/categories/CategoryGrid.tsx</files>
  <action>
  Replace raw `<a>` tags with tracked wrappers in these components:

  **HeroSection.tsx (Server Component):**
  1. Import PhoneLink from '@/components/analytics/PhoneLink' and DirectionsLink from '@/components/analytics/DirectionsLink'
  2. Replace the `<a href="tel:+14342238163" ...>` CTA with `<PhoneLink phone="+14342238163" location="hero" className="..." ariaLabel="...">` preserving the existing className and aria-label. Move the SVG icon and "Call Us" text inside as children.
  3. Replace the `<a href="https://www.google.com/maps/dir/..." ...>` CTA with `<DirectionsLink destination="5301 Farmville Rd, Farmville, VA 23901" location="hero" className="...">` preserving className. Move the SVG icon and "Get Directions" text inside as children.

  **ContactSection.tsx (Server Component):**
  1. Import PhoneLink and DirectionsLink
  2. Replace the phone `<a href="tel:+14342238163" ...>` (line ~59-65) with `<PhoneLink phone="+14342238163" location="contact_section" className="..." ariaLabel="...">`. Keep "(434) 223-8163" as children.
  3. Replace the directions `<a href={...google maps...} ...>` button (line ~89-100) with `<DirectionsLink destination="5301 Farmville Rd, Farmville, VA 23901" location="contact_section" className="...">`. Keep the SVG icon and "Get Directions" text as children.
  4. Remove the `encodedAddress` variable and related logic since DirectionsLink handles encoding internally.

  **Footer.tsx (Server Component):**
  1. Import PhoneLink
  2. Replace the phone `<a href="tel:+14342238163" ...>` (line ~63-69) with `<PhoneLink phone="+14342238163" location="footer" className="..." ariaLabel="...">`. Keep "(434) 223-8163" as children.

  **Navigation.tsx (Client Component -- already has event handlers):**
  1. Import { sendGAEvent } from '@next/third-parties/google' directly (Navigation is already a Client Component, no wrapper needed)
  2. Add onClick handler to the DESKTOP phone link (line ~68-74): `onClick={() => sendGAEvent('event', 'phone_click', { phone_number: '+14342238163', click_location: 'nav_desktop' })}`
  3. Add onClick handler to the MOBILE phone link (line ~124-133): `onClick={() => sendGAEvent('event', 'phone_click', { phone_number: '+14342238163', click_location: 'nav_mobile' })}`

  **CategoryGrid.tsx (Server Component):**
  1. Import CategoryLink from '@/components/analytics/CategoryLink'
  2. Replace the `<a>` tag inside the categories.map() with `<CategoryLink href={cat.href} categoryName={cat.name} className="..." style={{ backgroundColor: cat.color }}>`. Move the gradient overlay div, category name h3, and arrow indicator span inside as children.

  IMPORTANT: Preserve ALL existing className, style, aria-label, and aria-current attributes exactly. The visual appearance and accessibility must not change. Only the tracking behavior is added.
  </action>
  <verify>
  Run `npm run build` -- no errors. Verify each updated component:
  - Grep HeroSection.tsx for "PhoneLink" and "DirectionsLink"
  - Grep ContactSection.tsx for "PhoneLink" and "DirectionsLink"
  - Grep Footer.tsx for "PhoneLink"
  - Grep Navigation.tsx for "sendGAEvent"
  - Grep CategoryGrid.tsx for "CategoryLink"
  All should be present. No raw `<a href="tel:` tags should remain in HeroSection, ContactSection, or Footer (Navigation keeps raw `<a>` but adds onClick).
  </verify>
  <done>
  All phone links tracked across HeroSection (hero), ContactSection (contact_section), Footer (footer), Navigation (nav_desktop, nav_mobile). All directions links tracked in HeroSection (hero) and ContactSection (contact_section). All category cards tracked in CategoryGrid (homepage_grid). Build passes. Visual appearance unchanged.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with no errors
2. Grep for raw `<a href="tel:` in HeroSection.tsx, ContactSection.tsx, Footer.tsx -- should be ZERO matches (all replaced with PhoneLink)
3. Grep for "sendGAEvent" in Navigation.tsx -- present (direct calls in client component)
4. Grep for "CategoryLink" in CategoryGrid.tsx -- present
5. Grep for "DirectionsLink" in HeroSection.tsx and ContactSection.tsx -- present
6. Start dev server, open browser DevTools Network tab, click a phone link -- should see gtag event in network requests (only if GA_MEASUREMENT_ID is set)
</verification>

<success_criteria>
- PhoneLink, DirectionsLink, CategoryLink wrappers exist in components/analytics/
- Every tel: link in Server Components uses PhoneLink with correct location parameter
- Every directions link uses DirectionsLink with correct location parameter
- Every category card uses CategoryLink with category name tracking
- Navigation phone links have direct sendGAEvent onClick handlers
- Build passes clean
- No visual or accessibility regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-seo-analytics-metadata/07-02-SUMMARY.md`
</output>
