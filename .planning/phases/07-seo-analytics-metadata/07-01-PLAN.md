---
phase: 07-seo-analytics-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/metadata.ts
  - app/layout.tsx
  - app/(main)/page.tsx
  - app/(main)/about/page.tsx
  - app/(main)/opengraph-image.tsx
  - app/(main)/about/opengraph-image.tsx
autonomous: true

must_haves:
  truths:
    - "FurnitureStore JSON-LD on all pages contains brand affiliations (La-Z-Boy, Ashley), priceRange, areaServed, paymentAccepted"
    - "About page has no duplicate FurnitureStore JSON-LD (only BreadcrumbList)"
    - "Every page has OpenGraph metadata with og:title, og:description, and og:image"
    - "Dynamic OG images render at 1200x630 with Town & Country branding"
    - "GA4 GoogleAnalytics component loads on every page via root layout"
  artifacts:
    - path: "lib/metadata.ts"
      provides: "Enhanced FurnitureStore JSON-LD generator"
      contains: "La-Z-Boy"
    - path: "app/(main)/opengraph-image.tsx"
      provides: "Dynamic OG image for homepage"
      contains: "ImageResponse"
    - path: "app/(main)/about/opengraph-image.tsx"
      provides: "Dynamic OG image for about page"
      contains: "ImageResponse"
    - path: "app/layout.tsx"
      provides: "Root metadata with openGraph + GA4 component"
      contains: "GoogleAnalytics"
  key_links:
    - from: "app/layout.tsx"
      to: "lib/metadata.ts"
      via: "generateOrganizationJsonLd import"
      pattern: "generateOrganizationJsonLd"
    - from: "app/layout.tsx"
      to: "@next/third-parties/google"
      via: "GoogleAnalytics component"
      pattern: "GoogleAnalytics"
    - from: "app/(main)/opengraph-image.tsx"
      to: "next/og"
      via: "ImageResponse import"
      pattern: "ImageResponse"
---

<objective>
Establish the complete metadata and analytics foundation for all pages: enhanced FurnitureStore schema.org structured data with brand affiliations, OpenGraph metadata with dynamically generated OG images, and GA4 bootstrapping via @next/third-parties.

Purpose: Makes the site discoverable by search engines, shareable on social media with branded preview images, and ready for event tracking.
Output: Updated metadata.ts, layout.tsx, page.tsx, about/page.tsx, two opengraph-image.tsx files, GA4 component in root layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-seo-analytics-metadata/07-RESEARCH.md

# Key source files
@lib/metadata.ts
@app/layout.tsx
@app/(main)/page.tsx
@app/(main)/about/page.tsx
@lib/fonts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance schema.org structured data and clean up About page JSON-LD</name>
  <files>lib/metadata.ts, app/(main)/about/page.tsx</files>
  <action>
  Update `generateOrganizationJsonLd()` in lib/metadata.ts to add these fields to the existing FurnitureStore JSON-LD:

  - `brand`: Array of two Brand objects -- { "@type": "Brand", name: "La-Z-Boy" } and { "@type": "Brand", name: "Ashley Furniture" }
  - `areaServed`: { "@type": "City", name: "Farmville", "@id": "https://www.wikidata.org/wiki/Q1399115" }
  - `priceRange`: "$$"
  - `paymentAccepted`: "Cash, Credit Card, Debit Card"
  - `currenciesAccepted`: "USD"

  Keep all existing fields (name, url, description, address, telephone, openingHoursSpecification) unchanged.

  In app/(main)/about/page.tsx:
  1. Remove the entire inline `structuredData` object (lines 19-48) and the `<script type="application/ld+json">` tag that renders it. The root layout already injects FurnitureStore JSON-LD on every page -- the About page copy is a duplicate that confuses search engines.
  2. Add a BreadcrumbList JSON-LD script instead. Import SITE_URL from lib/metadata.ts. The breadcrumb has two items: position 1 "Home" with item = SITE_URL, position 2 "About" with no item property (current page convention).
  3. Fix the openGraph.url from "https://townandcountryfurniture.com/about" to use `${SITE_URL}/about` for consistency.
  </action>
  <verify>
  Run `npm run build` -- no build errors. Inspect the output of generateOrganizationJsonLd() by checking lib/metadata.ts contains "La-Z-Boy", "Ashley Furniture", "areaServed", "priceRange", "paymentAccepted". Verify about/page.tsx no longer has a second FurnitureStore script tag, but does have a BreadcrumbList script tag.
  </verify>
  <done>
  generateOrganizationJsonLd() returns FurnitureStore with brand, areaServed, priceRange, paymentAccepted. About page has BreadcrumbList JSON-LD and no duplicate FurnitureStore JSON-LD. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure OpenGraph metadata and create dynamic OG images</name>
  <files>app/layout.tsx, app/(main)/page.tsx, app/(main)/about/page.tsx, app/(main)/opengraph-image.tsx, app/(main)/about/opengraph-image.tsx</files>
  <action>
  **Root layout metadata (app/layout.tsx):**
  Add `openGraph` to the existing metadata export:
  ```
  openGraph: {
    type: siteMetadata.og.type,
    locale: siteMetadata.og.locale,
    siteName: siteMetadata.og.siteName,
    title: siteMetadata.title,
    description: siteMetadata.description,
    url: SITE_URL,
  },
  twitter: {
    card: siteMetadata.twitter.card,
    title: siteMetadata.title,
    description: siteMetadata.description,
  },
  ```
  Do NOT add `images` here -- the opengraph-image.tsx file convention will auto-inject the image tags.

  **Homepage metadata (app/(main)/page.tsx):**
  Add `openGraph` to the existing metadata export:
  ```
  openGraph: {
    title: 'Town & Country Furniture | Farmville, VA',
    description: 'Farmville, Virginia\'s home for La-Z-Boy and Ashley Furniture.',
    url: SITE_URL,
  },
  ```
  Import SITE_URL from lib/metadata.ts. No images key needed (opengraph-image.tsx handles it).

  **About page metadata (app/(main)/about/page.tsx):**
  The openGraph config already exists but lacks og:image. Remove the hardcoded url, use `${SITE_URL}/about`. No images key needed (opengraph-image.tsx handles it).

  **Dynamic OG image - Homepage (app/(main)/opengraph-image.tsx):**
  Create a new file using Next.js opengraph-image.tsx file convention:
  - Export `alt`, `size` (1200x630), and `contentType` ('image/png')
  - Export default async function that returns `new ImageResponse(...)` from `next/og`
  - Design: Dark gradient background (oklch brand colors -- use something like linear-gradient from a dark warm tone to slightly lighter), large "Town & Country Furniture" heading in white, subheading "La-Z-Boy & Ashley Furniture | Farmville, VA", warm accent color strip at bottom
  - Use system fonts only (no custom font file loading needed for v1 -- keeps bundle under 500KB). Use `fontFamily: 'serif'` for heading, `fontFamily: 'sans-serif'` for subheading.
  - Layout: flexbox, centered content, padding 60px

  **Dynamic OG image - About (app/(main)/about/opengraph-image.tsx):**
  Same structure as homepage but with:
  - Title: "About Town & Country Furniture"
  - Subheading: "Family-Owned Since Day One | Farmville, VA"
  - Slightly different gradient angle or accent for visual distinction

  IMPORTANT: opengraph-image.tsx uses ONLY flexbox (no CSS Grid). Only inline styles. The `display: 'flex'` must be set on every container div.
  </action>
  <verify>
  Run `npm run build` -- builds successfully with no errors. Check that app/(main)/opengraph-image.tsx and app/(main)/about/opengraph-image.tsx exist. Verify the build output mentions these OG image routes. Check layout.tsx metadata export contains `openGraph` key.
  </verify>
  <done>
  Root layout, homepage, and about page all have OpenGraph metadata. Two opengraph-image.tsx files generate branded 1200x630 OG images. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Install @next/third-parties and add GA4 to root layout</name>
  <files>app/layout.tsx</files>
  <action>
  1. Run `npm install @next/third-parties@latest`
  2. In app/layout.tsx, import GoogleAnalytics: `import { GoogleAnalytics } from '@next/third-parties/google';`
  3. Add the GoogleAnalytics component AFTER the closing `</body>` tag but BEFORE closing `</html>` tag (this is the documented Next.js pattern). Conditionally render only if env var exists:
  ```tsx
  {process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID && (
    <GoogleAnalytics gaId={process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID} />
  )}
  ```
  4. Create a `.env.local` file (if it doesn't exist) or add to existing one: `NEXT_PUBLIC_GA_MEASUREMENT_ID=` (empty value -- client provides the actual G-XXXXXXXXXX ID later). If .env.local already exists, append the line only if the key doesn't already exist.
  5. Add `NEXT_PUBLIC_GA_MEASUREMENT_ID` to `.env.example` if that file exists, or create it with this single entry plus NEXT_PUBLIC_GOOGLE_MAPS_API_KEY and RESEND_API_KEY (document all env vars the project needs).

  The GoogleAnalytics component automatically tracks pageviews on client-side navigation. No manual pageview tracking code needed. It also enables GA4 Enhanced Measurement features including automatic scroll depth tracking at the 90% threshold -- no custom scroll tracking code is required.
  </action>
  <verify>
  Run `npm run build` -- no errors. Grep app/layout.tsx for "GoogleAnalytics" to confirm import and usage. Run `npm ls @next/third-parties` to confirm package installed. Check that .env.example contains NEXT_PUBLIC_GA_MEASUREMENT_ID. Note: scroll depth tracking is automatic via GA4 Enhanced Measurement once the GoogleAnalytics component is loaded and Enhanced Measurement is enabled in the GA4 property settings -- no custom implementation needed.
  </verify>
  <done>
  @next/third-parties installed. GoogleAnalytics component in root layout, conditionally rendered based on NEXT_PUBLIC_GA_MEASUREMENT_ID env var. Build succeeds. Env var documented in .env.example. Scroll depth tracking is automatic via Enhanced Measurement.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with no errors
2. Grep lib/metadata.ts for "La-Z-Boy", "Ashley Furniture", "areaServed", "paymentAccepted" -- all present
3. Grep app/(main)/about/page.tsx for "BreadcrumbList" -- present
4. Grep app/(main)/about/page.tsx for "FurnitureStore" -- NOT present (duplicate removed)
5. Grep app/layout.tsx for "openGraph" and "GoogleAnalytics" -- both present
6. Confirm app/(main)/opengraph-image.tsx and app/(main)/about/opengraph-image.tsx exist
7. `npm ls @next/third-parties` shows package installed
</verification>

<success_criteria>
- FurnitureStore JSON-LD includes brand, areaServed, priceRange, paymentAccepted
- About page has BreadcrumbList, no duplicate FurnitureStore
- Root layout, homepage, and about page all export openGraph metadata
- Dynamic OG images generate at 1200x630 for homepage and about page
- GA4 GoogleAnalytics component present in root layout
- @next/third-parties package installed
- Scroll depth tracking covered by GA4 Enhanced Measurement (automatic, no custom code)
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/07-seo-analytics-metadata/07-01-SUMMARY.md`
</output>
